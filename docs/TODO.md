# TODO — Backlog & Follow-Ups

The single aggregation point for open work. Work-logs track *how* a piece of work
moves through the pipeline; this file tracks *what's open* across everything.

**Reconciliation rule (Workflow Rule 10):** any commit that ships, defers, or
discovers work must update this file *in the same commit*. Phase 6 follow-ups
land here. Review punch-list items that get accepted land here. Shipped items
move to Done with the date. Claude reads this file at session start alongside
the cadence check.

Format: one line per item — `- [ ] <item> — <source/link>`. Keep it lean;
detail lives in the linked doc, not here. **Next Up and Backlog are ordered by
priority within each subsection** (highest first), not by date — check the
subsection heading, not position in the file, to gauge urgency.

---

## Verification debt (DECISION-045)

Phases 5 and 6 deferred on the foundation pipelines, to be cleared in one
operator-led pass once the foundation is coherent. Each line names what has not
been independently verified.

- [ ] **P0 — post-login router** (`603f0a1`): qa Phase 5 and analyst Phase 6 both deferred. Mechanical gates all green — 505 unit, 63 e2e, 59 isolation assertions, build clean — and the operator walked the destination matrix by hand.
- [ ] **`drizzle/0015_presby_membership_probe.sql` — different in kind.** A SECURITY DEFINER probe fixing `withOrgContext()`, written by a ux-developer mid-UI-slice, third occurrence of the F26 pattern, touching the isolation model. Wants a database-admin read regardless of when the rest is verified.
- [ ] **Slice A's `db:push` finding**: drizzle-kit renders CHECK constraints table-qualified while Postgres renders them unqualified with a cast, so all three checks read as drift on every push — including two that predate this work. Verify by `drizzle-kit pull` + snapshot compare instead.
- [ ] **P0.5 slices b/c/e** (`bf85bc4`…`d1e1427`) — the ramp generator, admin-only brand storage/emission, type pairings. Phases 1–4 complete, each commit independently re-verified by the orchestrator, DECISION-054–059 resolved. Phase 5/6 deferred to the same combined operator pass.
- [ ] **P1 — tenant permissions + org portal shell** (`564144f`…`9018eaa`): the `active_membership` derived group, the `directory.view` permission, `/o/<slug>/directory`. Phases 1–4 complete, DECISION-060–065 resolved, every commit independently re-verified (61/61 isolation assertions, 96/96 e2e, real-browser confirmation that hidden fields never appear in served HTML). **QA note**: no `scripts/seed-dev.sql` fixture is currently loginable with a real password/TOTP enrolment at a `managed` org with real directory content — manual verification this session used a temporary, fully-reverted scratch credential. A permanent, clearly-labeled dev-only loginable fixture (distinct from the existing non-loginable `elder.fixture`) would remove this friction for QA and any future manual pass.

## In Flight

- [x] **Migration-numbering near-collision, caught before commit (2026-08-27): `drizzle/0039`/`drizzle/0040` landed concurrently from two unrelated Phase 4 schema pipelines in the same working tree.** `docs/work-log/2026-08-27-staff-and-personnel.md`'s database-admin commit claimed `drizzle/0039_presby_staff_and_personnel.sql` after an `ls drizzle/` showing `0038` as the highest number; `docs/work-log/2026-08-27-feature-categories.md`'s own schema commit landed `drizzle/0040_presby_org_feature_categories.sql` in the same tree shortly after, with its `drizzle/meta/_journal.json` entry initially registered at `idx 39` (a mismatch against its own `0040` filename). No filename collision resulted — `0039` and `0040` are both genuinely free, distinct numbers — but `_journal.json` needed reconciling: the feature-categories entry was corrected to `idx 40` and the staff-and-personnel entry inserted at `idx 39` ahead of it, restoring idx/filename agreement for both. Named here per the 0031/0035-0036 precedent (CLAUDE.md's "sequence explicitly, don't let two pipelines guess the same number" discipline) rather than silently resolved. — `docs/work-log/2026-08-27-staff-and-personnel.md` Phase 4
- [ ] **`drizzle/0033_presby_groups_administration.sql`'s widened DELETE-branch trigger blocks a live org's own cascade-delete, not just a direct row delete (found running Phase 4 commit 2's own test suite, 2026-08-26).** `group_memberships.organization_id` has its own direct `ON DELETE CASCADE` FK to `organizations` (`src/lib/db/domain/groups.ts`), so deleting an organization cascades straight through every still-open derived `group_memberships` row that org's officer terms/memberships created — exactly the row-with-`source='derived'` case the widened trigger now correctly rejects on a DIRECT delete, but a cascade cannot distinguish itself from one. **Confirmed reproducible with a minimal, isolated repro** (insert an org + a derived Session/Active-Membership group + one `officer_terms` row, then attempt `delete from organizations`: fails every time with the trigger's own error). `src/lib/groups.test.ts`'s own teardown works around it (`alter table ... disable trigger`, teardown-only, never application code). `src/lib/officers.test.ts`'s own pre-existing `afterAll` (unmodified by this pipeline) leaves an equivalent open derived row (its F22 regression test's `term2` is deliberately never ended) and reproduced the identical failure at least once this session — but re-running it repeatedly afterward did NOT reproduce it, so there is a second, unexplained factor (most likely a Neon read-after-write timing/consistency wrinkle across connections, not disproof of the underlying mechanism, which the isolated repro confirms is real and deterministic). Needs a database-admin read: either every real-DB-backed test fixture that creates a derived row must adopt the same disable-trigger teardown, or the org-deletion cascade path itself needs a v2 fix distinguishing "an app call deleted this row directly" from "cascading from the org's own deletion." No real org-deletion feature exists in the app today — only test fixtures hit this — but the shared dev database will otherwise keep silently accumulating undeleted fixture orgs. — `docs/work-log/2026-08-26-groups-admin.md` Phase 4 commit 2
- [x] **Migration-numbering collision, caught before commit (2026-08-26): `drizzle/0031` was independently claimed by two concurrent Phase 4 schema commits.** `docs/work-log/2026-08-26-member-sensitive-info.md`'s full-stack-developer commit landed `drizzle/0031_presby_sensitive_info_permissions.sql` on disk (uncommitted) before `docs/work-log/2026-08-26-role-permissions-admin.md`'s database-admin commit checked the directory — neither `docs/TODO.md`'s In Flight list nor `drizzle/meta/_journal.json` (last entry 0030) surfaced the collision in advance. Resolved by renumbering the role-permissions-admin migration to `drizzle/0032_presby_role_definitions.sql`; no content change. Exactly the v0.6-wave near-collision CLAUDE.md's Common Commands section already warns about — worth a sharper mechanical check (e.g. a pre-commit `ls drizzle/ | sort | uniq -c` guard on the numeric prefix) rather than relying on each pipeline to `ls` at the right moment. — `docs/work-log/2026-08-26-role-permissions-admin.md` Phase 4
- [ ] **`resolvePushTargetRepoRoot()`'s `cd`-tracking may not reliably resolve `payload.cwd` inside a forked/background-agent execution context** — worked correctly when tested directly in the main session, but a background fork hit the hook incorrectly gating a `scratch/presby-site-kit` push again despite the fix being present on disk, specifically when the push command used a leading `cd` rather than an explicit `git -C <path>`. Workaround confirmed reliable: use `git -C <path> push` instead of `cd <path> && git push`. Needs a regression test that exercises the hook from exactly that execution context, not just the main session's shell. — `docs/work-log/2026-08-26-pre-push-gate-target-repo.md`
- [x] **Resolved 2026-08-26**: the `presby-site-kit@3.4.0` missing-exports claim above no longer reproduces — `buildPageMetadata`/`buildSitemapEntries`/`RenderSiteBundleInput.organizationName` are all present in the currently installed package, and `next build`/`tsc` are clean. The real, narrower bug was `sitemap.xml/route.test.ts`'s own mock pages carrying empty `frontMatter: {}` — `buildSitemapEntries()` correctly excludes any page without `frontMatter.navLabel` (the same opt-in signal `Nav` uses), so the test's fixture, not the code, was wrong. Fixed by adding `navLabel` to the mocked pages and a new case asserting a `navLabel`-less page is excluded. Whatever caused the original tag/dist mismatch this entry described either was never real or self-resolved before this session; no `presby-site-kit` change was needed.
- [x] **Portal chrome (org-named header + portal menu)** — SHIP IT (2026-08-25, full pipeline incl. one QA loop-back on the 360px truncation contract, resolved as a deliberate wider-fitting-is-better call with a stronger e2e contract). OrgMark replaces the "presby" wordmark inside `/o/<slug>`; persistent flag-filtered menu row; `org_portal.chrome_v2` + `org_portal.feedback` seeded OFF (deliberately ON in the dev DB for the fpcw demo). `(account)` branding split to its own Next Up line. Awaiting commit. — `docs/work-log/2026-08-25-portal-chrome.md`

- [ ] **P0 — post-login router and org context** — shipped and pushed. Phases 5 and 6 **deferred by DECISION-045**, not skipped; see Verification debt above. — `docs/work-log/2026-08-18-backbone-and-org-sites.md`
- [x] **Org-branded sign-in** — SHIP IT (2026-08-24, full six-phase pipeline incl. one Phase-6 loop-back for the Google-button recolor, fixed via the `[data-brand-neutral]` neutral-reset in `<BrandTokens>`). `/signin` is the third emitter (DECISION-094 amending DECISION-047), gated on `ui.branded_signin`, seeded off. Awaiting commit. — `docs/work-log/2026-08-24-branded-signin.md`
- [x] **Portal Home + Directory v2 (Households, Deacons)** — SHIP WITH NOTES (2026-08-24, full six-phase pipeline; QA PASS 1907 mocked + 93 DB-backed + RLS §18). All four increments shipped behind `org_portal.home_v2` / `org_portal.directory_v2` seeded OFF; `directory.view_hidden` permission with fixture-only bindings. Notes folded into Next Up below. Awaiting commit. — `docs/work-log/2026-08-24-portal-home-directory.md`
- [x] **Member management, Increment 2 (edit person)** — SHIP WITH NOTES (2026-08-26, full pipeline; self-verified — see Next Up caveat below). `updatePerson()`/`getPersonForEdit()` in `src/lib/people.ts`, `/admin/members/[id]/edit` (single-screen form, not a wizard — every value is a revisit of a known one, no duplicate-match/roll-action step to coordinate), Edit link on `admin/members`'s cards. Rides the existing `people.manage` permission and `org_portal.members_create` flag/toggle — no new gating axis. `AUDIT_ACTIONS.PERSON_UPDATED` added. 62 new tests (10 DB-backed, incl. an RLS cross-org proof). Awaiting commit. — `docs/work-log/2026-08-26-member-management-edit-person.md`
- [x] **Members & Directory — pagination, search, status filter (Increment 5)** — SHIP WITH NOTES (2026-08-26, full pipeline; self-verified). `/admin/members` gained search + status filter (it had neither); `/directory` gained status filter (search already existed, wired since Increment 3); both gained pagination (25/page) via a new shared `src/components/shared/pagination.tsx`. Query layer: `directoryEligibilityWhereSql()`/`queryDirectoryRows()` gained `status`/`page`/`pageSize`, a new `countDirectoryRows()` sharing the same WHERE-building, `getDirectory()`'s result gained an optional `pagination` field — all opt-in, byte-identical for the three narrower callers (`getHouseholdDetail`/`getPersonDetail`/`getParishRoster`) that never pass the new options. `DIRECTORY_STATUSES`/`DirectoryStatus` moved to a new `src/lib/directory-status.ts` (no `server-only` guard) so presentational components can use them without transitively pulling in the DB-access module. 82/82 DB-backed directory tests pass (14 new, incl. an RLS cross-org proof and a pagination-metadata proof), 75 new/updated mocked tests pass. Awaiting commit. **Correction (2026-08-26):** this pipeline's own `Pagination` component shipped with two hand-rolled button-shaped class strings, a real `check:brand-scope` C2 violation the pipeline's own "self-verified" pass missed — the tripwire wasn't run at ship time. Independently rediscovered by three unrelated pipelines the same day before being traced to this one and fixed: `pagination.tsx` now renders through the `<Button>` primitive (`variant="ghost"`, `asChild` for the live-link case, `disabled` for the end-of-range case), preserving the same `min-h-11`/`min-w-11` touch targets. `check:brand-scope` now passes tree-wide; existing `pagination.test.tsx` (5/5) unaffected. — `docs/work-log/2026-08-26-members-directory-pagination-search.md`

## Next Up

### Bugs & security-adjacent gaps

- [ ] **Another instance of the recurring shared-dev-DB test-pollution problem (see the `roll.test.ts` line just below for the first): a leaked `memberships` row for a person named "Testy Verifyington" (`created_at = 2026-08-27 21:21:28`, Alder Creek org) is not part of `scripts/seed-dev.sql`'s fixture and broke `scripts/test-rls.sql`'s section-2 count assertion (`alder: sees own memberships — expected 9, got 10`), aborting the full suite (`ON_ERROR_STOP`) before it could reach the `staff_positions`/`officer_terms` sections.** Found verifying `drizzle/0041_presby_public_staff_directory.sql` (schema-only migration, does not touch `memberships` — confirmed unrelated by direct inspection). Worked around by running a targeted, pollution-immune isolation proof instead of the full suite for that step's own verification (all passed — see the work-log). Not cleaned up here — the row belongs to whichever concurrent pipeline's test teardown leaked it, and identifying/fixing that pipeline is out of scope for a schema-only commit. This is now at least two independent leaks from concurrent DB-backed test runs against the same shared dev database in one day; worth treating as a pattern (a pre-verification `scripts/test-rls.sql`-blocking sweep, or per-pipeline DB isolation) rather than cleaning one row at a time. — `docs/work-log/2026-08-27-public-staff-directory.md` Phase 4 step 1
- [ ] **Regression: `src/lib/roll.test.ts`'s `afterAll` no longer disables `group_memberships_reject_derived` before cascading its `platform.delete(organizations)`.** docs/TODO.md's own 2026-08-26 log records this exact fix landing and passing 1/1 clean; direct read of the current file (2026-08-27, presbytery-oversight-statistics schema commit) shows only the pre-existing `roll_actions_freeze` disable/enable wrap — the `group_memberships_reject_derived` wrap is absent, so the file's own `active_membership`-derived group_memberships row (created at line ~138) blocks the teardown every time, failing the suite (all 20 real tests still pass; only the `afterAll` throws) and leaving an orphaned `roll-test-a-<stamp>` organization + its people/users in the shared dev database on every run. Reproduced twice during this schema commit's verification pass (unrelated to this commit — no file this pipeline touches is implicated); 4 orphaned fixture orgs plus their people/users/role_grants/memberships/group_memberships/roll_actions found and cleaned from the shared dev database in the same pass. Needs a bug-fix pipeline: re-add the disable/enable wrap `read-org-brand.test.ts` still carries correctly (line ~218) as the reference shape. — found `docs/work-log/2026-08-27-presbytery-oversight-statistics.md` Phase 4
- [ ] **SECURITY (residual, out of scope of the 2FA-bypass fix) — the identical Server-Action redirect-inlining mechanism also defeats `proxy.ts`'s `PROTECTION_RULES` feature-gate checks** (e.g. `FEATURES.ADMIN_USERS`), not just the 2FA gate, for a *non-2FA-required* user landing on a feature-gated `/admin/*` path directly via `callbackUrl` in one hop. The 2FA-bypass fix (`docs/work-log/2026-08-24-totp-callback-bypass-fix.md`) only evaluates the 2FA predicate inside `signInWithCredentials` — it does not duplicate `PROTECTION_RULES` (that would be materially larger scope than the Sev-1 fix). Needs its own Phase 1 — a distinct (if related) finding, not a re-statement of the 2FA bug. — `docs/work-log/2026-08-24-totp-callback-bypass-fix.md` Phase 3 Edge Cases
- [ ] **No confirmation guard on granting a `role_grants.manage`-carrying role to a group (including a derived group like `active_membership`) — only the symmetric revoke-side self-lockout case is guarded.** A `stated_clerk` misclicking the group target instead of a named person in `grant-role-form.tsx` could hand role-administration power to every current member in one submit — recoverable via revoke, not exploitable by an unauthorized actor, but a real gap against the pipeline's own "no wildcard, no accidental over-grant" bar. Recommend excluding derived groups from the group `<select>` when the selected role carries `role_grants.manage`, or a named confirmation step mirroring the revoke dialog's specificity — `docs/work-log/2026-08-19-tenant-administration.md` Phase 6
- [ ] **F19 — build `terminate_person_participation(person_id, as_of, reason)` (or equivalent trigger)** so an approved `death`/`removed_by_session`/`renunciation_of_jurisdiction`/`certificate_dismissed`/`affiliate_ended`/`other_participant_removed`/`other_loss` roll action actually ends open `officer_terms`, `role_grants`, and `group_memberships` as of the effective date. `docs/schema-design.md`'s F19 line was found still Open (mismarked "Applied §8") while designing `2026-08-26-member-roll-on-edit`, corrected the same day. Needed before any UI (including that pipeline's own edit-time roll-action form) can safely offer a terminating `roll_action_kind` — database-admin, own dated pipeline, needs a real isolation/adversarial review since it requires `SECURITY DEFINER` to see across the officer/grant axes. — `docs/work-log/2026-08-26-member-roll-on-edit.md` Phase 2/3
- [ ] **`createPerson()` (`src/lib/people.ts`) inserts its `roll_actions` row with neither `resulting_roll` nor `age_at_action` set** — every person created through the `/new` wizard gets `current_roll = null` on approval instead of `active`/`other_participant`, since `presby_sync_current_roll` reads `resulting_roll` straight off the inserted row. Real, currently-shipping bug, unrelated to F19 — discovered while designing `recordRollAction()`'s own insert for `2026-08-26-member-roll-on-edit`, not fixed there since it means touching `createPerson()`. — `docs/work-log/2026-08-26-member-roll-on-edit.md` Phase 3
- [ ] **Pre-existing circular-import ordering defect: a bare top-level `import { X } from "@/lib/db/domain/<submodule>"` can throw `"<enum-name> is not a function"` at module-evaluation time** — reproduced with `@/lib/db/domain/roll`, whose transitive chain reaches `domain/authz.ts` before `domain/org.ts`'s own `organizationType` pgEnum export has initialized, when the submodule is the entry point rather than the `@/lib/db/domain` barrel (`index.ts`, which orders `./org` first). Confirmed pre-existing on `main` HEAD, unrelated to any change in this pipeline — every other test file avoids it only because DB-backed suites without `DATABASE_URL` skip before their `beforeAll` dynamic imports ever run, so it had never surfaced. Worked around locally in `src/lib/roll-action-kinds.test.ts` (imports via the barrel instead); the underlying module-graph ordering is unfixed. — discovered writing `2026-08-26-member-roll-on-edit` Phase 4
- [ ] **`person_relationships` still has no DB-level CHECK constraint enforcing `related_person_id IS NOT NULL OR related_name IS NOT NULL`.** `docs/schema-design.md`'s original design specified one; `drizzle/0008_presby_domain.sql` never applied it. Children's ministry Increment A closes the gap at the app layer (`addGuardianLink`/`updateGuardianLink`'s own XOR validation) — the first application code to touch this table at all — but the DB-level gap itself is unfixed; a direct INSERT bypassing the app still can land a fully-null row. — `docs/work-log/2026-08-26-childrens-ministry.md` Phase 3 Edge Cases
- [ ] **`person_relationships`' INSERT policy still has no check on `related_person_id`** — DECISION-111 named this "a thin existence-oracle, not F21-shaped" and left it unfixed at the RLS layer; Increment A narrows it at the app layer instead (`addGuardianLink`/`updateGuardianLink` verify the related person holds an active membership at the acting org before allowing the write), which is a real mitigation but not a DB-level fix. — `docs/work-log/2026-08-26-childrens-ministry.md` Phase 3 Edge Cases / DECISION-111
- [ ] **`check:audit`'s `MUTATION_RE` regex only matches literal `db.insert/update/delete`, not `tx.`/`platformDb.`** — a file using only `tx.insert`/`platformDb.execute` and forgetting `recordAudit()` would pass the tripwire silently. Two files now share this gap (`admin/2fa/actions.ts`, `admin/organizations/[id]/actions.ts`); broaden the regex at the next monthly code review — `docs/work-log/2026-08-19-brand-foundation.md` Phase 4 commit `c2`

### Blocking a flag flip / go-live

- [ ] **Go-live gate: flip every remaining `org_portal.*` product-IA placeholder flag to `enabled: false` before any real congregation or presbytery is onboarded.** Seeded ON deliberately for dev/demo visibility (Phase 1 Operator Answer 4) — giving, worship, committees, communications. **Shrank by three (2026-08-27):** `org_portal.oversight`/`.reports`/`.insights` graduated out of this block — real schema (`drizzle/0038_presby_presbytery_program.sql`) now sits behind them, so they follow the ordinary seeded-off convention instead of waiting for this sweep. The loud comment block in `scripts/seed.ts` names each remaining one; flip the whole block, not one flag at a time, so none is missed. — `docs/work-log/2026-08-27-product-ia-scaffold.md` Phase 3 / `docs/work-log/2026-08-27-presbytery-oversight-statistics.md` Phase 4
- [ ] **No Playwright e2e spec covers the new portal-home/directory/household/parish routes** — safe today only because `org_portal.home_v2`/`org_portal.directory_v2` ship seeded OFF; an e2e spec must exist before either flag flips ON in any real environment — QA Phase 5 gap 1, `docs/work-log/2026-08-24-portal-home-directory.md`
- [ ] **No Playwright e2e spec covers the public-staff-directory OR committee-directory click-through flow (toggle → confirm → server action → DB → public render)** — safe today only because both `sites.public_staff_directory` and `sites.public_committee_directory` ship seeded OFF; an e2e spec covering both `staffDirectory` and `committeeDirectory` `liveSlot` resolvers must exist before either flag flips ON for any real organization. Real-DB integration tests + a documented, screenshot-backed real-browser walkthrough exist for both and substantively cover the same claims, but that isn't a substitute for committed automated regression coverage. — QA Phase 5 advisory / Phase 6 analyst, `docs/work-log/2026-08-27-public-staff-directory.md`, `docs/work-log/2026-08-28-public-directory-primitives.md`
- [ ] **No Playwright e2e spec covers the new org-feature-categories picker on `/o/<slug>/admin/features`** — safe today only because `org_portal.feature_categories` ships seeded OFF; an e2e spec must exist before the flag flips ON in any real environment, matching the identical precedent above. Live-browser/360px verification WAS done (Phase 6), but that isn't a substitute for automated regression coverage of the default-on resolver, the category-off disabled state, and preserve-not-reset. — Phase 6 analyst, `docs/work-log/2026-08-27-feature-categories.md`
- [ ] **Live-browser 360px verification of the sensitive-info sub-forms** (`/o/<slug>/admin/members/<id>/edit/sensitive`) before `org_portal.sensitive_info` is ever flipped on for a real congregation — not yet done, per CLAUDE.md's "Verify in a Browser" invariant. — `docs/work-log/2026-08-26-member-sensitive-info.md` Phase 6
- [ ] **Live-browser 360px verification of `RecordRollActionForm`** (`/o/<slug>/admin/members/<id>/edit`) before `org_portal.members_roll_action_edit` is ever flipped on for a real congregation — reuses already-verified `EditPersonForm` conventions so risk is low, but no actual phone-viewport check has been done yet, per CLAUDE.md's "Verify in a Browser" invariant. — `docs/work-log/2026-08-26-member-roll-on-edit.md` Phase 6
- [ ] **Member management edit-person — not independently verified in a real browser at mobile width.** Unlike Increment 1's own Phase 4/6, this increment's mobile-UX claims (single column, native inputs, 44px targets) rest on typecheck + automated suite only, no real dev-server click-through. Do one before this ships to real users: sign in as `stated_clerk`, edit a real seeded person, confirm at 360-390px. **Correction (2026-08-26): `stated_clerk`/`clerk.fixture@example.invalid` requires 2FA enrollment to sign in — this instruction's own assumption (a plain password sign-in) doesn't hold. Complete the 2FA enrollment first, or use a different fixture that both holds `people.manage`/`directory.view` and signs in without 2FA.** — `docs/work-log/2026-08-26-member-management-edit-person.md` Phase 6
- [ ] **Members & Directory pagination/search/status (Increment 5) — same real click-through gap, same fixture obstacle.** `org1@presby.invalid` genuinely lacks `directory.view` at its own org (`e2e-alpha`) — confirmed live, correctly renders forbidden, just not useful for exercising the new UI. `clerk.fixture@example.invalid` (Alder Creek, holds the right permissions) hit the same 2FA-enrollment wall above. A real click-through — search, status filter, and pagination together, at both desktop and ~390px — is required before this ships to real users. Worth fixing the underlying fixture gap once (a loginable, 2FA-free fixture that holds `directory.view`/`people.manage` at a real org) rather than re-discovering it per pipeline. — `docs/work-log/2026-08-26-members-directory-pagination-search.md` Phase 5
- [ ] **Neither `clerk.fixture` nor `elder.fixture` (Alder Creek) carries a persisted TOTP enrolment, and Alder Creek's `organization_settings.require_two_factor = true` blocks BOTH from a real browser sign-in until one is completed by hand via `/account/2fa`.** Discovered during the support-tickets pipeline's Phase 4 commit 3 real-browser verification — `elder.fixture` was made sign-in-capable (a password, mirroring `clerk.fixture`'s own P9 precedent) specifically to walk `/o/alder-creek/tickets` and `/o/alder-creek/feedback`, and hit this 2FA wall immediately; worked around for that one verification session with a scratch, throwaway TOTP secret seeded directly against the dev database, not committed anywhere. A permanent fix mirrors `e2e/support/totp-fixture.ts`'s exact approach (AES-256-GCM against `AUTH_TOTP_ENCRYPTION_KEY`, the same published demo secret) added to `scripts/seed-dev.sql` or a sibling script — out of scope for this pipeline to build, since it's fixture-infrastructure, not ticket-UI work. Same underlying gap the P1 QA note above (`docs/TODO.md`'s Verification-debt section) already named for a *loginable* fixture; this is its 2FA-enrolment half — `docs/work-log/2026-08-20-support-tickets.md` Phase 4 commit 3.
- [ ] **Live-walk the credentials empty states.** The transferring-in-TE empty-person-list and zero-member-congregation states are in source and test-covered but were never exercised in a live click-through (every dev fixture person already holds a presbytery membership). Quick confirmation next time the form is touched. — `docs/work-log/2026-08-26-presbytery-functionality.md` Phase 6

### Product decisions needed

- [ ] **`publicDisplayOrder` is a single global per-row ordering value, not a per-marker/per-page pin** — a deliberate, reasoned Phase 2 tradeoff against ever exposing a row id in the anonymous public projection (DECISION-132/133). Works cleanly for every use case named so far (a curated leadership page, per-committee pages) because each draws from a disjoint data set. If a future request needs the SAME person to appear in two differently-ordered curated arrangements (e.g. a homepage "top 3" widget and a full leadership page with a different order), this mechanism can't express that without either a second ordering column or an id-list-in-marker redesign — flagged for whoever picks that request up. — `docs/work-log/2026-08-28-public-directory-primitives.md` Phase 6

- [ ] **`visiblePortalTiles()` doesn't gate tile visibility on the per-org feature toggle (`organization_feature_toggles`) at all today, and the new org-feature-categories axis (in design) deliberately inherits the same gap rather than being the first axis to fix it.** An org that toggles `org_portal.members_create` off still sees the Members tile — the wizard/action denies inline instead. Confirmed by both Phase 1 (analyst) and Phase 2 (architect) of the feature-categories pipeline as a real, pre-existing, deliberately deferred-to-v2 gap, not something this pipeline is scoped to fix. — `docs/work-log/2026-08-27-feature-categories.md` Phase 1 Point 5 / Phase 2 re-run
- [ ] **The DB-role/code rename to PresbyPortal — a large, scoped future pipeline, not a piecemeal edit.** The project is now named (DECISION-126, `presbyportal.org`), but `presby_app` (the Postgres role every FORCE RLS policy and SECURITY DEFINER function references by name), the thirteen `presby_*` SQL functions, every migration filename, and `package.json`'s starter name all still use the placeholder. Needs its own Phase 1 to enumerate the full blast radius before any rename begins — a live role rename has session/connection implications. Public-facing copy uses PresbyPortal starting now regardless. — `docs/decisions.md` DECISION-126, `docs/STATE.md`
- [ ] **`ORG_FEATURE_CATALOG` (`/o/<slug>/admin/features`, the per-org self-service toggle axis, DECISION-097) is stale at 2 entries against ~30 `org_portal.*` flags that now exist.** Only "Add & approve members" and "Tiered sensitive information" were ever added; every feature shipped since (groups, roles, tickets, officers, branding, credentials, children's ministry, events, oversight, reports, the coming-soon set) is global-only, no per-org self-service control. Expanding this needs `orgTypeScope`-style filtering (DECISION-117's pattern) before it can grow — a congregation must never see a presbytery-only toggle (Oversight, Credentials) as an option. Real, worth scoping, not urgent. — operator, live-driving v0.20.0, 2026-08-27, `/o/northern-reach/admin/features`
- [ ] **Officers at a presbytery — deliberate product decision needed.** Nothing structurally forbids a presbytery role holding `officers.manage` (unlike credentials, which is walled by the template's `organization_type_scope`), so the Officers tool renders at presbytery orgs by default. Decide whether session/diaconate officer terms are meaningful for a presbytery or whether the tile should be org-type-scoped like credentials — a product ruling, not a bug. — `docs/work-log/2026-08-27-credentials-tile-org-type.md` Phase 1/6
- [ ] **Brand-token read/write asymmetry.** `getOrgBrandForLayout()` calls `generateBrandTokens(row.seedHex)` on every request and never reads `organization_brands`' stored `brand_token_version` — a generator change (e.g. `BRAND_TOKEN_VERSION` 1→2, this pipeline) silently re-renders every branded org's live tokens on next page load, with no migration step and no history row recording the change. Pre-existing property (architect ruling, Phase 2), surfaced again rather than fixed here. Needs a product/architectural decision: pin the read path to the org's stored version and require an explicit re-save to adopt a new one, or accept unconditional regeneration as the design and document it as such so it stops being independently rediscovered. — docs/work-log/2026-08-27-button-modernization.md Phase 2/3
- [ ] **`/o/<slug>/feedback`'s own flag gate (`org_portal.tickets`) disagrees with the avatar-menu item's visibility flag (`org_portal.feedback`).** Found during the product-IA scaffold's feedback relocation (Phase 3) — the two can be independently toggled, producing a menu item that leads to an "isn't turned on yet" page. Not fixed here (operator directed the route to survive unchanged); needs a decision on which flag is authoritative. — `docs/work-log/2026-08-27-product-ia-scaffold.md` Phase 3
- [ ] **New worshiping communities are invisible to presbytery oversight/statistics/per-capita.** `congregation_oversight`/`congregation_statistics`/`per_capita_records` all validate `aboutOrgId` against `organizationType = 'congregation'` only — an NWC is rejected as `invalid_target`, narrower than Increment 2's `appointments` precedent (which includes NWC as a valid serving org for a minister). Fails closed, documented in three places, not a defect — but a real open product question: does a presbytery want to track a promising NWC's viability/statistics before it particularizes as a congregation? Needs its own Phase 1 if the answer is yes. — `docs/work-log/2026-08-27-presbytery-oversight-statistics.md` Phase 6
- [ ] **`(account)` pages branding** (user feedback 2026-08-25: "account settings don't follow the branding scheme") — analyst's proposed rule: brand `/account*` with the user's org brand when they have exactly ONE enterable org (mirrors the `/launch` matrix philosophy, reuses the same query), platform palette for 0 or 2+. Split out of the portal-chrome pipeline by the architect (Phase 2 ruling): it's a fourth `EMITTERS` exception + a DECISION-094-style amendment, categorically heavier than chrome. Needs its own work-log/pipeline. — `docs/work-log/2026-08-25-portal-chrome.md` Phases 1-2
- [ ] **`/totp`, `/forgot-password`, and `/reset-password` stay platform-chrome even when the visitor arrived via a branded `/signin`** — a deliberate non-goal of the initial increment (Phase 1 Gap, Phase 3 design), producing a brand-then-platform-chrome flash mid-flow for a 2FA-enrolled member or anyone resetting a password from a branded entry point. Needs its own decision on whether these pages should inherit the org brand once a slug is known (and, if so, its own `EMITTERS` entries in `scripts/check-brand-scope.mjs`) or intentionally reset to platform chrome — not fixed here. — `docs/work-log/2026-08-24-branded-signin.md`
- [ ] **Dark-mode support should be an operator-configurable choice when setting up a congregation's branding, not an unconditional feature of every brand.** Today the ramp generator always derives both a light and a dark token set from one seed colour (`src/lib/brand/generate.ts`) — there's no way for an operator to say "this congregation's brand only makes sense in light mode" or otherwise opt an org out of the dark variant. Raised 2026-08-21, wants scoping soon rather than deferred indefinitely — needs its own Phase 1 to work out what "optional" means (an org-level flag that suppresses the dark ramp entirely? forces `next-themes` to `light` for that org's `(org)` pages? something at the admin brand-editor UI level only?) — no work-log yet, next `/new-feature` candidate for the brand surface.
- [ ] **`role_grants.roleId → appRoles.id` is `onDelete: cascade` — a standing latent risk.** Any future code path that hard-deletes an `app_roles` row (a script, a platform-admin tool, or a naive future feature) silently destroys every `role_grants` row referencing it, including *ended* (historical) grants — the exact append-only audit trail `revokeRole()`'s own contract protects. Recommend `onDelete: restrict` in a future migration. Found during the role-permissions-admin pipeline's Phase 2 review (DECISION-106), which deliberately avoids ever exercising this path (soft-deactivate only), but the FK itself is unfixed. — `docs/work-log/2026-08-26-role-permissions-admin.md` Phase 2
- [ ] **Tenant branding (`brand_admin`/`branding.manage`) — no org is auto-provisioned with a `brand_admin` holder.** Same bootstrap-gap shape as `stated_clerk`/`officers.manage` (DECISION-100), out of scope to fix here. — `docs/work-log/2026-08-26-tenant-branding-permission.md` Phase 3
- [ ] **Tenant branding — a brand change made through the new tenant path (`/o/<slug>/admin/branding`) does not revalidate a live public site's cached render.** `(org)` route handlers have no access to `organization_sites` (no `presby_app` grant, DECISION-081) and cannot call `getPlatformDb()`; only the platform brand form's `revalidateLiveSitePath()` can invalidate the cache today. A congregation that rebrands via self-service may see stale public-site colours until the next content ingest or a platform-operator re-save. — `docs/work-log/2026-08-26-tenant-branding-permission.md` Phase 3, DECISION-103
- [ ] **`tickets.file`'s sole holder can be revoked with no warning and no DB guard, unlike `role_grants.manage`.** A deliberate trade (DECISION-072's correction, `2026-08-20-role-catalog`) — splitting `tickets.file` off `stated_clerk` onto its own role gives up the emergent self-lockout protection piggybacking bought for free, since `revokeRole()`'s guard checks only `role_grants.manage`. Recoverable (a `role_grants.manage` holder can always re-grant it), and `property_chair` already lives with the identical exposure today, but worth a UI warning on revoke if this proves to matter in practice — `docs/work-log/2026-08-20-role-catalog.md` Phase 1
- [ ] **A missing `person_privacy` row defaults to the columns' own declared defaults (all visible except `hide_birthday`), not to full exclusion — load-bearing until a self-service privacy page exists to ever populate the row for real.** DECISION-064. Owned by whichever pipeline first builds member self-service profile editing — `docs/work-log/2026-08-19-tenant-permissions-portal.md` Phase 4 commit 2
- [ ] **P2 replaces `/no-organization`'s second door.** "Is your church not on presby?" currently links to `/` because there is no onboarding request form yet. When P2 ships one, point it there — the zero-org page is the funnel and a link to a marketing stub is the weakest part of it — `docs/work-log/2026-08-18-backbone-and-org-sites.md` Phase 4 slice C2
- [ ] **S17's account-level dark-mode persistence is unbuilt.** Ships a `next-themes` device-level `localStorage` mechanism only (DECISION-050) — no toggle UI ships yet, correctly, but when one does it must not be read as satisfying S17's "account-level, defaulting to system" ask until the `users` column + server-rendered initial class lands. Phase 6 follow-up — `docs/work-log/2026-08-19-brand-foundation.md`
- [ ] **`group_memberships.officer_term_id` has no foreign key** — a bare, unconstrained `uuid` column, exactly the shape Composite Tenant Keys exists to prevent (F2: a row in one org pointing at a person in another). Predates this pipeline; caught while designing the parallel `active_membership` derived group, which gets a real composite FK its sibling lacks. Candidate for a future database-admin review, not P1's to fix — `docs/work-log/2026-08-19-tenant-permissions-portal.md` Phase 2
- [ ] **`/developer` will not show `presby_membership_is_active` or the DECISION-039 triggers**, the same blindness already filed above for functions/triggers/policies — it is now three functions and three triggers behind, and one of them is the tenant gate

### Ready to build (named follow-ups)

- [ ] **Directory export/output capabilities are missing entirely** — PDF generation, photo/headshot management (upload + display), vCard export, and Google Maps directions links from a member's address, all present in the `../fpcw-directory` reference app, none built in presby's own Directory yet. **Printing/exporting the full directory must be permission-gated, not available to every directory viewer by default** — a real information-leakage risk (every member's address/phone/email in one exportable/printable document) if left open to the whole congregation the way plain `directory.view` is. Needs its own Phase 1 to scope: most likely a distinct `directory.export`-shaped permission separate from `directory.view`, with print/PDF and vCard export sharing that gate (same underlying leakage risk) while photo management and map-links are evaluated separately (different risk profile — a map link only reveals what a single card already shows). — operator, live UI comparison against `../fpcw-directory`, 2026-08-28
- [ ] **Children's ministry Increment B — consent + medical intake workflow.** Builds on existing `consents`/`person_medical`; needs an org-scoped program-settings table (waiver text, photo-consent language, program year — NOT a single-tenant singleton), the consent-tier ruling (tier 2 vs 3), and staff-entry-first per the operator's decision. Own Phase 2 onward when picked up. — `docs/work-log/2026-08-26-childrens-ministry.md` Phase 1/6
- [ ] **Children's ministry Increment C — check-in/check-out. UNBLOCKED** — events Increments 1–2 shipped 2026-08-27 (`docs/work-log/2026-08-26-events-model.md`, SHIP WITH NOTES). Re-run its own Phase 1 against the now-shipped frozen six-column contract (DECISION-113: `events.id`/`organization_id`/`starts_at`/`ends_at`/`cancelled_at`/`allows_checkin`). — `docs/work-log/2026-08-26-childrens-ministry.md`
- [ ] **Presbytery Increment 1 — committees, commissions, and administrative-commission tracking.** Deferred by the operator's "0 + 2" choice; starts its own Phase 1 when picked up. — `docs/work-log/2026-08-26-presbytery-functionality.md` Phase 1 / DECISION-112
- [ ] **Presbytery Increment 4a — statistical publication (congregation→presbytery).** Publication mechanism now exists: `presby_publish_sasr_snapshot()`/`presby_list_own_congregation_publications()` (SECURITY DEFINER, DECISION-118) shipped in the same schema commit above, ships dark behind `org_portal.statistical_publication` (seeded off) per Operator Answer 2. **Remaining:** api-developer (server action + audit) then ux-developer (publish form + history UI) — work-log `docs/work-log/2026-08-27-statistical-publication.md`.
- [ ] **Presbytery Increment 4b — presbytery dashboard/rollups.** Depends on 3b/4a's UI landing first; the viability map slice is additionally gated on an architect Phase 2 dependency addendum (Leaflet + tile-provider choice) before implementation starts — see `docs/work-log/2026-08-27-presbytery-program.md` Phase 3 Edge Cases. Work-log `docs/work-log/2026-08-27-presbytery-dashboard.md`.
- [ ] **Presbytery Increment 5 — imports and reports.** Deferred; starts its own Phase 1 when picked up. — `docs/work-log/2026-08-26-presbytery-functionality.md` Phase 1
- [ ] **A per-parish detail page (real member roster) would let the Parishes tab's district panels link somewhere.** `getParishRoster()` (`src/lib/directory.ts`) only returns aggregates today (deacon, household count) — no per-parish member list exists to drive a detail view. The 2026-08-26 portal-ux-fixes batch (H4) resolved the dead-end-card finding by demoting the panels to a non-interactive `bg-muted/40` info treatment rather than building this (feature-class, out of a polish batch's scope) — a real detail page + reader is the fuller fix if/when there's appetite for it. — `docs/work-log/2026-08-26-portal-ux-fixes.md` Wave 1C
- [ ] **Feedback page: show a member their own past submissions and status.** `/o/<slug>/feedback` is a pure write-only form even though its copy promises "a designated role-holder reviews everything shared here" — a member has no way to check back. Feature-class (needs a new read path + list UI), deferred out of the 2026-08-26 portal-ux-fixes polish batch. — `docs/reviews/2026-08-26-portal-ux.md` L4
- [ ] **Staff-hiring's inline "add a new person" path doesn't call `matchPerson()`/`presby_match_person()` first** — a person who already exists as a `people` row elsewhere (e.g. an existing member being hired as paid staff) gets a duplicate, unlinked row instead of being matched, unlike `admin/members/new`'s own dedup discipline. Not a Phase 3 v1 requirement — named and correctly flagged by the implementer, not silently shipped. — `docs/work-log/2026-08-27-staff-and-personnel.md` Phase 6

### Papercuts & small fixes

- [ ] **The "Coming soon" pill wraps awkwardly under a long tile title at 375px** (`Worship & Service Planning`, `TileGrid`) — cosmetic, worth a look next time that card is touched (truncate the title first, or stack the pill below it on narrow widths). — `docs/work-log/2026-08-27-portal-tile-contrast-and-coming-soon.md` Phase 6
- [ ] **`docs/testing.md` says `clerk.fixture@example.invalid` isn't gated behind a separate TOTP-enrolment detour** — stale; Alder Creek's `organization_settings.require_two_factor` is now `true` in the shared dev database, contradicting it (discovered during the staff-and-personnel pipeline's own browser verification). Low priority, documentation-only. — `docs/work-log/2026-08-27-staff-and-personnel.md` Phase 6
- [ ] **`scripts/test-rls.sql`'s full-file run aborts partway through section 3 ("Global person tables are gated by membership, not by a column compare") in the shared dev database, blocking every section after it (including any new section added by a same-day pipeline) from ever running as part of the full file — only standalone/extracted runs of later sections currently work.** Root cause confirmed live: a third, untracked `memberships` row for `admin@presby.invalid` was added directly against the shared dev database during an earlier, unrelated session (the presbytery-portal walk, Increment 0) and never carried into `scripts/seed-dev.sql` — the file's own comment at the failure point already documents this. It is live-data drift, not a schema-state or migration issue, so it will not self-resolve as more migrations land. Fix: either add the missing `seed-dev.sql` row so the fixture matches reality, or reset the drifted row back to match `seed-dev.sql`'s intent — whichever is judged the correct steady state. — found independently by qa in both `docs/work-log/2026-08-27-feature-categories.md` and `docs/work-log/2026-08-27-staff-and-personnel.md` Phase 5, 2026-08-27
- [ ] **`/admin/design-system` (the brand-token sign-off page) wasn't re-screenshotted against the new PresbyPortal palette (DECISION-127).** It exists specifically to show every token at once — a natural next stop, not done in that pipeline's scope. — `docs/work-log/2026-08-27-presbyportal-brand-kit.md` Phase 6
- [ ] **The PresbyPortal brand-kit pipeline ran self-implemented and self-verified, not through the six-phase agent pipeline** (Classification: Polish, finished asset kit rather than an ambiguous feature) — same honest caveat several other same-day Polish pipelines have carried. A genuinely independent look (architect on the token/contrast math, qa on the browser verification) would be the fuller version if the palette proves to need another pass. — `docs/work-log/2026-08-27-presbyportal-brand-kit.md` Phase 6
- [ ] `presbyportal_logo_stacked.svg` and the square `PlatformMark` component ship with no live call site yet — narrow-space and icon-only treatments the brand kit provides for, unused today. Not dead code; note only. — `docs/work-log/2026-08-27-presbyportal-brand-kit.md` Phase 6
- [ ] **`/admin/sites` (gated `FEATURES.ADMIN_ORGANIZATIONS`) has no tile in the new `ADMIN_TILES` registry** — the same class of gap the Tickets fix closed for this pipeline, named but explicitly out of scope (adding it would be scope creep on a design asked to fix exactly two named defects). Reachable only by URL/sidebar nav today; add a tile next time `src/lib/admin-portal/tiles.ts` is touched. — `docs/work-log/2026-08-27-platform-home-and-portal.md` Phase 3 Edge Cases
- [ ] **`src/app/(admin)/admin/layout.tsx`'s left-sidebar `nav` array is a hardcoded, unfiltered 12-item list** — the identical "shows everything regardless of what's held" defect this pipeline fixed for the `/admin` tile grid (`visibleAdminTiles()`), unaddressed in the sidebar. A `support_operator` still sees all 12 sidebar links even though only 2 destinations admit them. — `docs/work-log/2026-08-27-platform-home-and-portal.md` Phase 3 Edge Cases
- [ ] **`e2e/support/routes.ts`'s visual-parity manifest still has a `path: "/orgs"` entry (org-multi storageState).** `/orgs` is now a `next.config.ts` permanent redirect to `/home` (DECISION-124), which technically violates this file's own selection rule 1 ("renders, never redirects") — the harness's `page.goto` follows the redirect and gets a real 200 from `/home`'s content, so the `expectedStatus` assertion still passes, but the captured screenshot is now `/home`'s merged content under the `orgs-*.png` filename, not the old chooser. Not fixed in `docs/work-log/2026-08-27-platform-home-and-portal.md` commit 2 — `routeSlug()` derives its filename purely from `path` with no `storageState` component, so simply renaming this entry's `path` to `/home` would collide with the manifest's existing `path: "/home"` (`storageState: "member"`) entry's screenshot filename. Needs a rename (e.g. give `routeSlug` a storageState-aware suffix, or drop this entry and confirm the existing `/home`+`member` entry's coverage is sufficient) plus a baseline re-capture (`npm run visual:baseline`), not a blind path edit. — `docs/work-log/2026-08-27-platform-home-and-portal.md` Phase 4 commit 2
- [ ] **Six `(org)/o/[slug]` error/not-found surfaces still link `/orgs` for their "Back to your organizations" affordance** (`error.tsx`, `not-found.tsx`, `org-states.tsx` ×2, `tickets/[id]/not-found.tsx`, `directory/[personId]/not-found.tsx`, `directory/households/[householdId]/not-found.tsx`). Functionally correct — `/orgs` 308s to `/home` (DECISION-124) — but each is one extra hop, and Phase 3's stale-link list deliberately scoped cleanup to `org-switcher.tsx`/`no-organization/page.tsx`/`proxy.test.ts` only. Cosmetic; update the six `href`s to `/home` directly next time one of those files is touched. — `docs/work-log/2026-08-27-platform-home-and-portal.md` Phase 4 commit 2
- [ ] **Lint: 7 pre-existing errors on main** — `react-hooks/set-state-in-effect` in `branding-form.tsx:100` and `portal-nav-links.tsx` (the close-menu-on-navigate pattern), `react/no-children-prop` ×5 in the children's roster files (shipped v0.18.0). Not push-gated today; clean up in a lint pass. — found during the 2026-08-27 batch verification
- [ ] **`children-roster-list.tsx:79` link-styled control still 14px/medium.** A `<Link>` styled as a bordered button now reads as a visibly smaller, lighter outlier beside the 16px/semibold Button primitives. Bump to `text-base font-semibold`, or better, convert to the Button primitive via `asChild`. — `docs/work-log/2026-08-27-control-legibility.md` Phase 6
- [ ] **`docs/ui-standards.md`: four raw-`<button>` snippets still teach `text-sm font-medium`** (Action Bar ~181, Four-State Component Design ~555, Empty States ×2 ~587), contradicting the updated control recipes in the same file. Update to `text-base font-semibold` or replace with `<Button>` primitive usage so the doc stops teaching a hand-rolled pattern that drifts. — `docs/work-log/2026-08-27-control-legibility.md` Phase 6
- [ ] **Dark mode: Input/Textarea vs native-select background mismatch.** The primitives carry `dark:bg-input/30` while every native `<select>` uses plain `bg-background` with no dark variant — in dark mode they render different surfaces side by side. Pre-existing (found by qa during the light-mode bg-background standardization, which deliberately didn't touch dark); fix is deciding the dark-mode control standard in docs/ui-standards.md and applying it to both. — `docs/work-log/2026-08-27-input-background-standard.md` Phase 5
- [ ] **Children's roster: persistent missing-DOB notice.** The no-DOB exclusion is disclosed only in the empty state — a populated roster shows no hint that children without a birthdate are invisible. Add "N children may be missing from this list — add a birthdate to include them." — `docs/work-log/2026-08-26-childrens-ministry.md` Phase 6
- [ ] **Guardian search: zero-results message.** `guardian-link-form.tsx`'s typeahead stays silently empty on no matches. — `docs/work-log/2026-08-26-childrens-ministry.md` Phase 6
- [ ] **Children's ministry roster — a person with no `dateOfBirth` on file is invisible to the roster, even under 18 in real life.** Named and accepted in Phase 3 (DECISION-111 ruled out a stored is-child flag): the age-cutoff query requires a non-null `date_of_birth`, so a congregation importing legacy records with no birthdate never sees that child on `/o/<slug>/admin/members/children` until staff add one through the existing edit form. Data-quality follow-up, not a design defect. — `docs/work-log/2026-08-26-childrens-ministry.md` Phase 3 Edge Cases
- [ ] **`role_grants` grant doesn't check for an existing active grant before inserting** — the same `(organizationId, roleId, person_id/group_id)` can be granted twice, producing two indistinguishable active `role_grants` rows with no warning. Data-quality only, not a security issue — `docs/work-log/2026-08-19-tenant-administration.md` Phase 6
- [ ] **`Switch` primitive (first use, `/admin/features`) throws a React hydration-mismatch console error under forced mid-hydration layout** — doesn't occur under normal load-then-interact timing (3 clean runs). Low priority, worth a look next time the primitive is touched. — Phase 6, `docs/work-log/2026-08-25-member-management.md`
- [ ] **Member management edit-person pipeline ran self-verified**, not through independent `analyst`/`architect`/`tech-lead`/`qa` subagents (named in its own Phase 5) — the next real touch of this code should get a genuine independent QA pass rather than re-running the same suite. — `docs/work-log/2026-08-26-member-management-edit-person.md` Phase 5
- [ ] **`NWC` (`new_worshiping_community`) has no explicit tile-scope test case.** Four of five `OrganizationType` values are exercised in `tiles.test.ts`'s orgTypeScope block; NWC rides the same proven `.includes()` path. QA rated it low-risk, not silently waived — add the fifth case next time the file is touched. — `docs/work-log/2026-08-27-credentials-tile-org-type.md` Phase 5
- [ ] **Tenant branding — rate-limiting parity with the platform brand form (neither has it) is accepted, but multiplying the actor count (every `brand_admin` holder at every managed org) changes the abuse-risk shape** even though per-actor risk is unchanged. Not blocking; worth a look if abuse is observed. — `docs/work-log/2026-08-26-tenant-branding-permission.md` Phase 3
- [ ] **`BrandForm`'s client-side `useState(initial...)` fields (seedHex, typePairing, and now lightOnly) don't reactively reflect a just-saved value** — after `setOrganizationBrandAction` succeeds and `revalidatePath` re-fetches the server component tree, the client `<BrandForm>` doesn't remount, so `useState`'s frozen initial value keeps showing the PRE-save state until a real page navigation. The save itself is correct (verified: a full `page.goto()` reload shows the persisted value) — this is a display-only staleness bug. Fix: key the form on a value that changes per save (e.g. `updatedAt`), or seed state from a `useEffect` keyed on the prop. **The fix pattern is now proven working**, not just theorized: `(org)/o/[slug]/admin/branding/branding-form.tsx` (the new tenant-facing sibling, `docs/work-log/2026-08-26-tenant-branding-permission.md`) adds exactly the `useEffect`-keyed-on-initial-props re-seed described here, verified live (a save reflects without reload). This platform `BrandForm` itself is still unfixed — the same patch just needs porting over. — found verifying `docs/work-log/2026-08-24-light-only-brand.md` Phase 4/5.
- [ ] **`BrandPreviewSwatch` shows no font at all for any type pairing** — the admin brand-editor preview renders the seed-colour swatch but never the resolved heading/body font, for all four curated pairings (classic/modern/warm/contemporary) alike, and gets more costly to keep deferring as the option count grows. Fixable without breaking DECISION-047: an inline style/class on the swatch's own text, the same way its colour swatch already does it. Pre-existing before this pipeline, not created or worsened by the fourth pairing — `docs/work-log/2026-08-24-custom-brand-fonts.md` Phase 2
- [ ] **`src/lib/sites.test.ts`'s `beforeAll` writes a platform-wide `group_types` "roster" row via `.onConflictDoNothing()`, but `group_types` has no unique constraint on `(organization_id, key)` — only `id` (always a fresh random UUID) is unique, so the conflict clause never fires.** Discovered while implementing org creation (`docs/work-log/2026-08-24-admin-org-create.md` Phase 4): the shared dev database now carries 82+ duplicate `organization_id IS NULL, key='roster'` rows accumulated from repeated test runs over time. Harmless functionally today (any consumer that looks up the template by key just needs *a* match), but real hygiene debt and a trap for the next feature that assumes `group_types` templates are singletons. Fix: either add the missing unique constraint (`database-admin`) or switch `sites.test.ts`'s `beforeAll` to the same find-or-create pattern `scripts/seed.ts`'s `seedGroupTypes()` now uses — `docs/work-log/2026-08-24-admin-org-create.md` Phase 4.
- [ ] **No automated regression test guards `scripts/seed.ts`'s `seedGroupTypes()` idempotency itself** — `scripts/seed.ts` has zero pre-existing test coverage repo-wide, and this is exactly the class of bug (`.onConflictDoNothing()` silently not being idempotent, since `group_types` has no unique constraint on `(organization_id, key)`) that already bit the codebase once for real in the line above. A future "simplification" of `seedGroupTypes()` back toward that pattern would ship silently with nothing to catch it. Bundle with the line above — both trace to the same missing-unique-constraint root cause — `docs/work-log/2026-08-24-admin-org-create.md` Phase 5/6.
- [ ] **`src/lib/rate-limit.test.ts`'s in-memory suite fails 3/15 when `.env.local` is loaded across the whole suite** (`dotenv -e .env.local -- npx vitest run`, no file filter) — originally reported as "cross-file pollution when run alongside a real-DB integration file," but **root-caused during `2026-08-20-public-sites` Phase 4 commit 4**: the actual trigger is `RATE_LIMIT_DISABLED=true` in `.env.local` (`src/lib/rate-limit.ts`'s own documented local-dev escape hatch) — confirmed by setting only `DATABASE_URL`/`PLATFORM_DATABASE_URL` (no `.env.local`, no `RATE_LIMIT_DISABLED`) and getting 15/15 clean, then loading `.env.local` alone with zero other files present and reproducing the same 3 failures. `rate-limit.test.ts`'s ordinary (non-escape-hatch) tests never isolate against this ambient env var. Does not affect the CI-equivalent `npx vitest run` with no `.env.local`/`DATABASE_URL` (clean). Pre-existing, not fixed here — the correct workaround is running `dotenv -e .env.local` scoped to individual DB-backed files (as every DB-backed suite's own documented command already does), never suite-wide — `docs/work-log/2026-08-19-tenant-permissions-portal.md` Phase 4 commit 2; root cause corrected in `docs/work-log/2026-08-20-public-sites.md` Phase 4 commit 4
- [ ] **Adopt `forbidden()` for the org access-denied page when it leaves `experimental.authInterrupts`.** DECISION-044(3) renders it at HTTP 200 today; the byte-identical-copy property DECISION-040 protects is satisfied, the status code is not — `docs/work-log/2026-08-18-backbone-and-org-sites.md`
- [ ] **`npm run db:push` reports permanent phantom drift and cannot be used as the "schema.ts matches the migration" proof.** Three causes, none introduced by P0: push is interactive and refuses to run without a TTY; it proposes re-adding `memberships_person_org_key`, which demonstrably exists; and drizzle-kit renders CHECK constraints table-qualified while Postgres renders them unqualified with a cast, so all three checks in the schema read as drift on every push. `drizzle-kit pull` into a scratch directory is the comparison that does work — either wire that up as the check or drop the claim from the design template — `docs/work-log/2026-08-18-backbone-and-org-sites.md` Phase 4 slice A. **Reproduced again during P0.5 `c1`** (2026-08-19): hangs on the same phantom `memberships_person_org_key` prompt before it ever reaches new-table detection.
- [ ] **`npm run db:generate` is broken repo-wide, a different failure mode from the `db:push` drift above.** `drizzle/meta/0009`–`0012_snapshot.json` are byte-identical copies of `0008`'s `id`/`prevId` — a real collision, not phantom drift — so drizzle-kit refuses to compute any diff at all. Migrations `0013`–`0017` have all been hand-authored with a manually-registered `_journal.json` entry as a workaround; either repair the snapshot chain or make the hand-authored pattern the documented house style in `CLAUDE.md`/`docs/schema-design.md` — `docs/work-log/2026-08-19-brand-foundation.md` Phase 4 commit `c1`. **`npm run db:migrate` is also broken, confirmed 2026-08-19 during `2026-08-19-tenant-administration` Phase 4 commit 1**: it exits 1 with no stderr, and `__drizzle_migrations` shows drizzle-kit has never successfully tracked anything past `idx 9` (`0009_presby_rls`) in the dev database — reproduced on unmodified `main` via `git stash`, so it predates this pipeline. `0013`–`0018` were evidently all applied by hand via `psql "$MIGRATE_DATABASE_URL" -f drizzle/00XX_*.sql` (the mechanism `docs/testing.md` documents), never through `db:migrate` itself. Worth naming explicitly wherever the snapshot-chain repair happens, since fixing `db:generate` alone won't fix this.
- [ ] **`npm run docs:erd` silently drops `organization_brands`/`organization_brand_history`/`blob_assets`** from its generated diagrams even while reporting all tables scanned — likely a fixed section-list (A–J) with no "storage"/"brand" bucket. Also surfaces pre-existing, unrelated drift (nullable-FK arrows on `roll_actions.proposed_by`/`officer_terms.recorded_by`, missing `groups` edges) that predates this work and belongs to the documentation review, not this pipeline — `docs/work-log/2026-08-19-brand-foundation.md` Phase 4 commit `c1`
- [ ] **The brand role vocabulary cannot express the card and popover surfaces.** `BRAND_ROLES` has one `surface` role, so `muted-foreground on card` — measured 4.70 light / 5.78 dark before the P0.5 slice-0 correction — is not reachable through `LEGAL_PAIRS`. It is asserted as a named exception inside `src/lib/brand/contract.test.ts` today. Either add `card-surface`/`popover-surface` roles or state in `ui-standards.md` that `surface` covers all three and the generator must hold the floor on each — `docs/work-log/2026-08-19-brand-foundation.md` Phase 4 commit `0.1`
- [ ] **`/admin/users` row order is unstable — the query needs a secondary sort key** (`orderBy(desc(users.createdAt), users.id)` in `src/app/(admin)/admin/users/page.tsx`). Seeded users share identical `created_at` values and Postgres tie order shifts on any UPDATE to the table, so a refresh can visibly reshuffle rows. Caught by the visual-parity harness on unmodified code (~3,600-pixel diff per capture); the route is excluded from `e2e/support/routes.ts` until fixed — `docs/work-log/2026-08-19-brand-foundation.md` Phase 4 commit `a2`
- [ ] **Type-scale conformance is unmigrated.** `TYPE_SCALE` (`src/lib/brand/contract.ts`) exists and new surfaces are authored to it, but 244 `text-sm` and 97 `text-xs` sites predate it and are not migrated — a larger, more visually consequential sweep than the primitive migration. Phase 6 follow-up — `docs/work-log/2026-08-19-brand-foundation.md`
- [ ] **`authz.ts` and `org.ts` have a circular import that breaks under Vitest when `domain/org` is imported first in isolation** — `authz.ts`'s top-level `organizationType("organization_type_scope")` call runs before `org.ts`'s own export is assigned, throwing `TypeError: organizationType is not a function`. Real app code never hits it (`src/lib/db/index.ts` always imports `./schema` first), and it's been worked around locally twice now (`blob-store.test.ts`, `organizations/[id]/actions.test.ts`) with an explicit `import "@/lib/db/schema"` ordering hack. Un-cycle `authz.ts`'s import of `org.ts`'s enum, or document the workaround once centrally instead of every future test file rediscovering it — `docs/work-log/2026-08-19-brand-foundation.md` Phase 4 commit `c2`
- [ ] **e2e cached `storageState` can go stale within the same session when `FEATURE_CATALOG` changes.** `hasFeature()` checks a `features[]` array baked into the session at sign-in, so a same-day permission addition (e.g. `FEATURES.ADMIN_ORGANIZATIONS`) can read as "you don't have permission" against a `<12h`-old cached admin session, even though the fixture holds `ADMIN_ROLE`. Worth a freshness check keyed to something more than a time window (hash the catalog) so this doesn't silently mask a real permission regression as "the feature doesn't work" — `docs/work-log/2026-08-19-brand-foundation.md` Phase 4 commit `c3`
- [ ] **G10's third onboarding door is partial** — "upload the logo and accept a sampled suggestion" (auto-extracting a seed colour from an uploaded logo) has no sampling step; only "pick a colour," "enter a hex code," and a plain (non-suggesting) logo upload exist in `/admin/organizations`. Pick up when slice d or a future `(admin)` polish pass revisits onboarding — `docs/work-log/2026-08-19-brand-foundation.md` Phase 4 commit `c3`
- [ ] **Wordmark upload has no write path.** `organizationBrands.wordmarkAssetKey` exists in the schema (`c1`) but `c2`'s `setOrganizationBrandAction` only wires the square `mark`. A deliberate scope call for the minimal (S18) operator surface, not an oversight — pick up when `c3`/slice d needs it — `docs/work-log/2026-08-19-brand-foundation.md` Phase 4 commit `c2`
- [ ] **`admin/users/[id]/deactivate-card.tsx` and `two-factor-card.tsx` hand-roll `@radix-ui/react-dialog` for destructive confirmations instead of the generated `AlertDialog` primitive** (`a4` regenerated it for exactly this). Migrate for consistency with the other three destructive-confirmation consumers (`account/delete-button.tsx`, `feedback-prompt-card.tsx`, `whats-new/delete-button.tsx`) and their stricter focus/escape/outside-click semantics — `docs/work-log/2026-08-19-brand-foundation.md` Phase 4 commit `a5`
- [ ] **`/account/2fa` and `/admin/audit` are time-sensitive visual-parity routes** — the TOTP pending-enrollment secret has a 10-minute TTL (the QR code repaints) and the audit log accretes live rows, so either route false-diffs on any check separated from its baseline by more than a few minutes. Needs a stabilization strategy (deterministic fixture / frozen clock) or exclusion, the `/admin/users` way — `docs/work-log/2026-08-19-brand-foundation.md` Phase 4 commit `a3`
- [ ] **Committed screenshot baselines in CI need a pinned container image** — the `a2` harness is deliberately self-comparing on one machine (DECISION-053); promoting it to CI is its own decision — `docs/work-log/2026-08-19-brand-foundation.md` Phase 3
- [ ] **`resolveOrgContext()` is not `cache()`-wrapped, so `(org)/o/[slug]/layout.tsx` (brand resolution) and `page.tsx` (`assertOrgAccess()`'s gate) each read the user's full membership list independently within one request.** Matches `cachedAuth.ts`'s precedent (wrap the export, no signature change) — deferred out of `c4` to keep that commit's diff scoped to `read-org-brand.ts` and the layout — `docs/work-log/2026-08-19-brand-foundation.md` Phase 4 commit `c4`
- [ ] **`--font-heading`/`--font-body` are declared brandable in `contract.ts` but nothing consumes them.** `c4` applies `fontPairing.bodyClassName` directly to `(org)`'s `<main>` (verifiable, working) rather than wiring the two CSS custom properties into `@layer base` `h1`–`h6`/body selectors, because `src/lib/brand/fonts.ts` (`e1`) only exposes `next/font`'s `.className`, not `.variable` — there is nothing for `var(--font-heading)` to resolve to yet. Heading and body currently render in the SAME face on `(org)` pages; a real heading/body split needs `fonts.ts` to also expose `.variable` classes plus the `@layer base` rule — `docs/work-log/2026-08-19-brand-foundation.md` Phase 4 commit `c4`
- [ ] **The org masthead (`GlobalNav`) does not render the organization's logo (`OrgMark`/`OrgWordmark`, already built in `c3`) on `(org)` pages.** `c4` wires colour and font tokens only; `GlobalNav` is a shared component (also rendered in un-brandable contexts) and wiring the mark in is a small, separate, reviewable change — `docs/work-log/2026-08-19-brand-foundation.md` Phase 4 commit `c4`

### Owed what's-new posts (drafted, deferred until each flag flips on)

- [ ] **What's-new entries at flag-flip: `org_portal.credentials`, `org_portal.events`, `org_portal.children_ministry`.** All three shipped seeded off, so Rule 13 correctly deferred their `whats_new_entries` rows — whoever flips each flag on for a real congregation/presbytery should publish the short entry then. — Phase 6 notes of all three 2026-08-26/27 pipelines
- [ ] A `whats_new_entries` post for the portal visual modernization pass (bold tiles/greeting, type-scale fixes, motion) is drafted-but-deferred, not forgotten. Every touched surface sits behind a pre-existing off flag (`org_portal.home_v2`/`chrome_v2`/`chrome_v3`) — publish when one of those is first flipped on for a real congregation, folding `org_portal.motion` into the same entry. — `docs/work-log/2026-08-26-portal-visual-modernization.md` Phase 6
- [ ] A `whats_new_entries` post for the Organization Administration hub is drafted-but-deferred, not forgotten. `org_portal.admin_hub` ships seeded off, so nobody can reach `/o/<slug>/admin` yet — publish the entry when the flag is actually flipped on for a real organization, not before. — `docs/work-log/2026-08-26-portal-reorg-and-modernization.md` Phase 6
- [ ] A `whats_new_entries` post for officer-terms administration is drafted-but-deferred, not forgotten. `org_portal.officers` ships seeded off, so nobody can reach `/o/<slug>/admin/officers` yet — publish the entry when the flag is actually flipped on for a real organization, not before. — `docs/work-log/2026-08-26-groups-and-officers.md` Phase 6
- [ ] A `whats_new_entries` post for groups and committee administration is drafted-but-deferred, not forgotten. `org_portal.groups` ships seeded off, so nobody can reach `/o/<slug>/admin/groups` yet — publish the entry when the flag is actually flipped on for a real organization, not before. — `docs/work-log/2026-08-26-groups-admin.md` Phase 6
- [ ] A `whats_new_entries` post for support tickets is drafted-but-deferred, not forgotten. `org_portal.tickets` ships seeded off, so nobody can reach `/o/<slug>/tickets*` or `/o/<slug>/feedback` yet — publish the entry when the flag is actually flipped on for a real organization, not before — `docs/work-log/2026-08-20-support-tickets.md` Phase 4 commit 3.
- [ ] A `whats_new_entries` post for tenant role administration is drafted-but-deferred, not forgotten. `org_portal.roles` ships seeded off, so nobody can reach the feature yet — publish the entry when the flag is actually flipped on for a real organization, not before — `docs/work-log/2026-08-19-tenant-administration.md` Phase 6

## Backlog

### Not designed yet — needs its own Phase 1

- [ ] **Session (and other governing-body) meeting minutes/dockets — no module exists.** Deferred at the schema level since D3: `roll_actions.minute_reference` is free text with no real FK, and no `meetings`/`docket_items`/`minutes` tables exist yet — see `docs/schema-design.md` §16 for the deferred shape (`meetings` → `docket_items` → `minutes`, tied back to roll approvals). Would also serve officer elections at congregational meetings, budget approvals, and presbytery minutes review — `docs/schema-design.md` D3/§16 name it as real, deliberately deferred work, not an oversight. Not started; scope with its own Phase 1 before any schema lands.
- [ ] **Ledger and giving** — requirements pass needed; `../westervillelions` (24 ledger tables) is the starting point, not the answer.
- [ ] **Worship planning and check-in** — not designed. Children's ministry Increment C (now unblocked, tracked above) will exercise the check-in half against the events model first.

### Known invariant violations & hygiene

- [ ] **Remove the `ADMIN_ROLE` wildcard** (`src/lib/permissions.ts`) — CLAUDE.md's own Key Invariants section names this as a still-open "known violation" of "No Role Carries a Wildcard." Bounded today (platform shell only, tenant connection can't bypass RLS) but not removed.
- [ ] **Photo-specific wiring for `people.photo_key` is still open** — the generic adapter DECISION-030 asked for is now built (`blob_assets` + `src/lib/storage/blob-store.ts`, DECISION-055/058, `docs/work-log/2026-08-19-brand-foundation.md` commit `c1`), with the logo as its first consumer. What's left: no photo has ever been migrated, `people.photo_key` itself is untouched, and the cloud-storage adapter is still deferred until scale demands it (F13 accepted knowingly). **Elevated to a priority follow-up as of 2026-08-27**: the public staff-directory feature's own name promises "headshots" it doesn't deliver — v1 shipped with photo upload deliberately unwired per DECISION-131, against Phase 1's explicit ruling that this should have been sequenced *before*, not after, that feature. The serving mechanism needs no further schema work (the existing `(public)/site/[slug]/assets/[key]/route.ts` already resolves any blob key by `organizationId`); what remains is upload UI + a `people.photo_key` mutation. Now also affects the new `PersonCard`/committee-directory rendering, which degrades gracefully with no photo the same way `StaffList` does — same underlying gap, not a new one. — `docs/work-log/2026-08-27-public-staff-directory.md` Phase 6, `docs/work-log/2026-08-28-public-directory-primitives.md` Phase 6
- [ ] **Household-grouped transfer claim flow (F20)** — not built.
- [ ] **D9 sequencing (congregation vs. presbytery priority) — likely moot in practice, worth a formal close-out.** Deferred 2026-08-18 as "neither is subordinate; revisit when the Roll UI has to pick an audience." Since then, both congregation-side (members/directory/groups/officers/sensitive-info) and presbytery-side (credentials/oversight/statistics/per-capita/appointments) feature sets have shipped extensively — the sequencing question appears to have resolved itself through parallel build-out rather than a deliberate choice. Worth a short decisions.md entry formally closing D9 next time schema-design.md is touched, rather than leaving it open.
- [ ] **A separate private repo for versioned private material.** `private/` stays untracked scratch only (gitignored + pre-commit-hard-blocked); nothing needing version control has moved there yet.

### Process / tooling

- [ ] Fable review C2: package the `.claude/` process layer as a Claude Code plugin (versioned `plugin.json`, marketplace-installable) — candidate successor to fork-and-copy + `upstream-sync` diffing
- [ ] Fable review C5: test-tampering ratchet (commit failing tests before implementation, tests frozen during Phase 4) + evaluate mutation testing as a future `check:*` tripwire
- [ ] Reconcile `.env.example` as the canonical env-var inventory — `DATABASE_URL_UNPOOLED`, `AUTH_TRUST_HOST`, `UPSTASH_REDIS_REST_URL/TOKEN`, `TRUST_PROXY_HEADERS`, `RATE_LIMIT_DISABLED` referenced elsewhere but absent from it; verify each against actual `process.env` usage — follow-up from `docs/reviews/2026-07-11-agent-instruction.md`
- [ ] Fable review C3: move auth/server-action rules into path-scoped `.claude/rules/*.md` to relieve CLAUDE.md load
- [ ] Fable review D5 nits: `fileURLToPath` in `functionality-map.mjs`/`check-audit-coverage.mjs` (Windows) · parameterize `stats-escape.mjs` `--since` (30d window vs 14d retro cadence double-counts)
- [ ] Fable review F8: escape-rate alarm sentence in tech-lead.md (post-merge % rising two retros running → propose a named tripwire) · state branch-protection/PR-required posture for `main` in CLAUDE.md · one-line-per-decision index atop `docs/decisions.md` · add deployment-engineer's "Pre-Deploy" section stub to `_template.md`
- [ ] Batch-bump routine low-risk deps (Radix, Tailwind patch, React types, Resend, otplib, lucide, tsx, Playwright, Vitest) in one PR — deps 2026-07-11
- [ ] Watch for `drizzle-kit@1.0.0` stable (resolves esbuild GHSA-67mh-4wv8-2f99; treat as major-version project with Neon branch smoke) — deps 2026-07-11; Next 16.3.0 taken 2026-08-09 (resolved the bundled-postcss CVEs; 48/48 e2e)
- [ ] Consider a `db.transaction(`-with-neon-http grep tripwire (mirror of check:sql-date) — the BUG-1 class; retro 2026-07-11 #2. **Largely moot for the app connection since the F28 driver switch** (neon-serverless supports transactions); close deliberately or re-scope to any remaining neon-http call sites — `docs/work-log/2026-08-18-stale-test-guards.md`
- [ ] Schema comment on `emailQueue` pointing at `RawQueueRow`/`fromRaw()` in `src/lib/email/queue.ts` so future columns don't silently skip the atomic-claim path — code 2026-07-11
- [ ] ADR for the "text, not pgEnum" status-column convention (or add DB CHECK constraints) — security 2026-07-11 (observational)
- [ ] Next release slot: run a standalone test-coverage sweep (don't rely on incidental Phase 5 numbers) — retro 2026-07-11 #6
- [ ] Harvest candidate: huddleup's `scripts/cadence-check.mjs` — SessionStart hook that computes overdue reviews and prints only the delta, replacing the manual read-log-and-compute ritual at session start — spotted during the 2026-07-12 functionality-map harvest
- [ ] Remaining OPEN items from the starter-contribution triage (C3/C4 QA gates, C7/C8 process docs, A7/A8 deploy ordering, D2/D3 CI+cron, E2 `dialog.tsx` + E3/E4 primitives, F2 usage heartbeat, …) — work via `docs/reviews/2026-07-01-starter-contribution-triage.md`; specs in `docs/starter-contributions/README.md`

### Deferred, low urgency

- [ ] Admin lock-state detail page — add lock badge + unlock action to `/admin/users/[id]` — follow-up from `docs/work-log/2026-07-02-admin-lock-visibility.md` Phase 3 scope decision
- [ ] `(password-reset)` error boundary — same gap as `(email-verify)`; unauthenticated token-lookup route, no error.tsx — tracked from `docs/work-log/2026-07-02-email-verify-error-boundary.md` Phase 1 Pass 5
- [ ] Tier 4 utilities on demand (csvCellSafe, ssrf-guard, magic-bytes, maskEmail, settings store, ConfirmDialog, iconKey nav, cf-connecting-ip, route-table 2FA gate, …) — harvest Tier 4
- [ ] TOTP enrolment e2e — requires either a seeded deterministic TOTP secret (security risk — see routing feature option (c) rationale, e2e-auth-infra work-log Phase 2 Ruling 7) or external authenticator integration; deferred until a safe pattern is designed

## Done

- [x] 2026-08-30 — **Secrets & PII pre-push tripwire, plus a one-time audit of the whole tracked tree now that presby is public.** New `npm run check:secrets` (`scripts/check-secrets-pii.mjs`, DECISION-134) scans every `git ls-files`-tracked file for private keys/cloud API-key shapes/JWTs/embedded credentials (HARD, no exemption) and env-var-shaped secrets/off-allowlist emails/phone-and-SSN-shaped numbers (SOFT, `// leak-ok: <reason>` exemptable), wired into `npm run check` and a new `/pre-push` Step 3e. 31 fixture tests (`check-secrets-pii.test.mjs`), matching `check-brand-scope.mjs`'s pure-function-plus-fixtures testing pattern. The one-time audit of the current tree found and fixed two real, pre-existing findings: a generated dev password narrated into a 2026-07-01 work-log's prose (redacted), and a real congregation's real address/phone entered into `docs/STATE.md` while testing the org-profile feature (redacted; the underlying dev-database row is unaffected and not itself public). Both fixes are working-tree redactions only — git history is left unrewritten, an explicit operator decision, not an oversight. — `docs/work-log/2026-08-30-secrets-pii-tripwire.md`

- [x] 2026-08-28 — **Public directory primitives (filterable staff/officer/committee data + composable presby-site-kit components)** — SHIP WITH NOTES. `getPublicStaffRoster()` grows an optional `filter` (kind/department/office/hasPriority); a new sibling `getPublicCommitteeRoster()` reads a new `SECURITY DEFINER` `presby_public_committee_roster()` over `group_memberships`/`groups` (per-membership opt-in, `groups.manage`-gated, derived-group write path guarded exactly like `endGroupMembership()`). A new admin-set `publicDisplayOrder` column (staff/officer/committee tables) gives content authors hand-curated, ordered subsets (`{"hasPriority": true}`) without ever exposing a row id in the anonymous projection. `presby-site-kit` v4.0.0 (breaking) turns `liveSlots` values into resolver functions `(filter) => ReactElement | null` so one slot name renders a different filtered subset per marker instance, plus a new `PersonCard` primitive (no contact-field props at all) for grouped, per-committee sections. Confirmed by direct construction at Phase 6: both operator-named examples — a hand-picked, ordered "leadership" page (`{"filter":{"hasPriority":true}}`) and per-committee pages (`{"filter":{"committee":"..."}}`) — are buildable today from the shipped API, verified in a real browser at desktop/390px/360px. New `sites.public_committee_directory` flag, seeded off, separate from `sites.public_staff_directory`. No per-person route, no id/group_id in the anonymous projection, field-scope enforced at both SQL and mapping layers, `recordAudit()` on the committee opt-in toggle (first audited call in `groups.ts`). Follow-ups: no committed e2e spec for the `committeeDirectory` flow yet (extends the existing staff/officer gap above); `publicDisplayOrder` is a single global order value, not a per-marker pin (Product decisions needed). — `docs/work-log/2026-08-28-public-directory-primitives.md`

- [x] 2026-08-28 — **Directory visual refresh (avatars, sub-tab tray, nav icons)** — SHIP IT. Three concrete gaps from a live screenshot comparison against `../fpcw-directory`, all closed: person avatars get a deterministic per-person color from a 6-hue palette instead of one flat identical circle (verified stable across reload and across two entry points); the Members/Households/Parishes sub-tabs move from plain bordered buttons to a rounded tray with a raised active pill (reusing the existing `ButtonGroup` primitive); the top nav gains a per-category icon (reusing the portal tile grid's own icon map) and a filled active pill instead of an underline. A real RSC-serialization bug (a Lucide icon component passed across a server/client boundary, 500-ing every nav page) was caught by live-browser verification, not `tsc`/`build`, and fixed by passing a string key instead — QA independently re-confirmed the fix holds at runtime. Verified on branded and default-palette orgs, dark mode, and both desktop and mobile widths. — `docs/work-log/2026-08-28-directory-visual-refresh.md`

- [x] 2026-08-27 — **Public staff & leadership directory** — SHIP WITH NOTES. An admin holding `staff.manage`/`officers.manage` can opt a specific, currently-open `staff_positions`/`officer_terms` row into a public "who serves here" listing (two new columns per table — `public_listed`/`public_listed_by`/`public_listed_at` — no join table, per-row not per-office); an anonymous visitor to an org's already-published `(public)/site/<slug>` sees the flat, alphabetized union wherever a content author has placed a `{"type":"liveSlot","props":{"slot":"staffDirectory"}}` marker in that org's MDX, via a new generic `liveSlots` injection mechanism shipped as `presby-site-kit` v3.5.0 (a minor, additive version bump in the sibling repo, confirmed durable under a fresh `npm ci`). Read is anonymous through a new narrow `SECURITY DEFINER` function (`presby_public_staff_roster`), with no per-person route ever built (enumeration-safe by construction, confirmed by direct code read at both Phase 5 and Phase 6); name/role/title/department/photo only, contact fields never selected, enforced at both the SQL projection and the render-mapping layer independently. `recordAudit()` fires on both toggle directions — the first audited mutation in either `staff.ts` or `officers.ts`, working around a real, confirmed blind spot in the `check:audit` tripwire (it only scans `actions.ts` files and never sees a mutation whose `recordAudit()` call lives one layer down in `lib/*.ts`) with real-DB-backed unit test assertions instead. Gated `sites.public_staff_directory`, seeded off. **Ships without photos** — `people.photo_key` renders gracefully null, a real drift from Phase 1's explicit ruling that a photo-less v1 under-delivers on the feature's own "headshots" framing and should have been sequenced behind a photo-wiring prerequisite rather than shipped without one; elevated to a priority follow-up (tracked above), not a someday item. A second Phase 6 finding — `getPublicStaffRoster()` had no fail-closed handling for a transient DB error, silently inheriting the riskier of two options Phase 1 said needed an explicit decision — was found and fixed the same day as a same-work-log bug-fix addendum (regression test confirmed failing pre-fix, passing post-fix). Resolves the "public staff/who to contact listing" follow-up filed under the staff-and-personnel pipeline. — `docs/work-log/2026-08-27-public-staff-directory.md`

- [x] 2026-08-27 — **Portal tile contrast + a distinct "coming soon" treatment** — SHIP WITH NOTES. Live-browser diagnosis against a real seeded org's own brand (`/o/fpcw`) found two concrete, screenshot-confirmed problems: the tool-tile icon badge (`bg-primary/10`) was nearly the lowest-contrast element on the page — a 10%-opacity brand tint on a page background D7 also permits tinting toward the same hue — while the avatar-menu circle (a corner affordance seen once) used a solid fill and read as more prominent than the badge repeated on every tile; and every "coming soon" stub tile rendered with identical visual weight to a real tool, diluting the page (4 of 10 visible cards on `/o/fpcw`'s own home were non-functional stubs). Fixed: live tiles get a solid `bg-primary`/`text-primary-foreground` badge; a new, documented, presentation-only `comingSoon` field on `PortalTile`/`TileLike` drives a muted badge + a `Coming soon` pill instead of the old un-structured description-text convention. Found and fixed a third, unrelated bug while reading the registry: `oversight`/`reports` tile descriptions still said "Coming soon" even though real, shipped functionality (the same day's presbytery-oversight-statistics pipeline) exists behind both routes — stale copy, corrected. — `docs/work-log/2026-08-27-portal-tile-contrast-and-coming-soon.md`

- [x] 2026-08-27 — **Staff and personnel — paid, non-ordained roles across congregation and presbytery** — SHIP WITH NOTES. New `staff_positions` table (record-keeping only for v1: position/title, department, dates, minute reference — no compensation, no public directory, per operator scope decision), composite-FK'd through `memberships` (F2) and F22-shaped GIST-excluded like `officer_terms`, deliberately orthogonal to ordinations/appointments (a unified "everyone who serves here" view is a read-time union, never a schema join). Resolves the DECISION-116 tension (an outside bookkeeper who never joins collides with "must already be a member first") not by amending DECISION-116, but by extending the shared `createPerson()` core with a `rollAction: { kind: "none" }` variant (DECISION-128) — no bespoke, unvetted person-creation surface, no fabricated roll event. **A real, previously-undetected bug was caught and fixed along the way** (DECISION-129): `createPerson()` hardcoded `engagementStatus: "regular"` unconditionally, which combined with the new no-roll-action path would have leaked non-member paid staff straight into the public member directory — now branches to `"staff"`, with dedicated regression tests proving exclusion from both `getDirectory()` and `findPersonMatches()`, independently confirmed live against the dev database. New `staff.manage` permission bound to a new `personnel_admin` role (DECISION-078's constitutional-duty test run and recorded against every existing office, all fail). Three-way implementer split (database-admin → api-developer → ux-developer) — the `createPerson()` extension's own blast radius justified the split, and it's what surfaced a second, self-found gap (the member-creation wizard could otherwise have had a staff-shaped `rollAction` smuggled into it). **A real QA FAIL cycle**, independently re-verified after remediation: a missing `staff.manage` check on the inline-create-new-person action (fixed, with a revert-then-restore-verified regression test), and zero `scripts/test-rls.sql` coverage for the new table (fixed, §31, including tenant isolation, the F2 rejection, the F22 exclusion, and an honestly-documented case-sensitivity limitation). Ships behind `org_portal.staff` (seeded off). Follow-ups: public staff-listing deferral and the missing-`matchPerson()`-on-inline-create gap (tracked above), `docs/schema-design.md` section (written in this commit), functionality-map entry (written in this commit), release notes (next `/release-notes` cut). — `docs/work-log/2026-08-27-staff-and-personnel.md`

- [x] 2026-08-27 — **Org feature categories — a fourth, org-chosen gating axis above the existing per-feature toggle** — SHIP WITH NOTES. Congregations/presbyteries can turn whole ministry areas (People & Membership, Worship & Events, Giving & Finance, Governance & Courts, Reports & Insights, Communications) on or off on `/o/<slug>/admin/features`, narrowing which per-feature toggles are even reachable — reusing DECISION-117's domain taxonomy for labels without repurposing `PortalTile.domain` itself into a gate (confirmed, not just asserted, by direct code read at every phase). **Genuinely default-on** (`categoryEnabledInTx()` returns `?? true`, loudly commented against reverting to the codebase's usual `?? false` convention) — an architect Phase 2 re-run overturned Phase 1's own AND-composition-plus-backfill recommendation specifically to eliminate the day-one silent-regression risk for orgs with an existing toggle already ON, rather than mitigate it after the fact. New `src/lib/db/domain/org-feature-categories.ts` + `src/lib/org-feature-categories.ts` (one-directional dependency from `org-features.ts`, never the reverse — DECISION-130), composed inside `isOrgFeatureEnabled()` so every existing call site inherits server-side enforcement for free. Reuses `org_features.manage`, with a hard-required per-`feature_key` audit-enumeration commitment in place of a dedicated permission key. New `org_portal.feature_categories` flag (seeded off) is a genuine kill switch — off, the axis is never even consulted. One real lint defect (JSX-in-try/catch) was caught by an independent QA FAIL and fixed; real-browser verification (all six of the implementer's named checks) was performed live against the dev database after both the implementer and QA reported having no browser-driving capability, catching and correctly ruling out one `router.refresh()`-timing false alarm before it could be misreported as a bug. Follow-ups: e2e coverage before the flag ever flips on (tracked above), functionality-map entry (done in this commit), release notes (next `/release-notes` cut). — `docs/work-log/2026-08-27-feature-categories.md`

- [x] 2026-08-27 — **PresbyPortal Brand Kit v1.0 applied to platform chrome and the marketing home page** — SHIP WITH NOTES. Navy/blue/gold palette (DECISION-127) repainted `PLATFORM_TOKENS`/`globals.css` in both schemes, every WCAG pair re-verified against S16's floors rather than trusted from the kit's README; `--destructive` nudged from the kit's literal `#B4232A` to `#B22B1E` after the ramp generator's 288-seed property test caught a real 0.1° OKLCH hue-distance near-miss (D6); dark mode is an original derivation (the kit ships one scheme only). Real logo (`PlatformWordmark`/`PlatformMark`, `src/components/brand/platform-mark.tsx`) replaces the "presby"/"PresbyPortal" text wordmark in `GlobalNav` and `/`, with a CSS-only light/dark swap fixed after a live browser check showed the light lockup nearly invisible on the dark header. New favicon/app-icon (Next.js icon conventions), Inter added at platform level (`next/font/google`), and a full-tree sweep fixed three more stray literal "presby" strings including the admin sidebar back-link the operator reported live. Regenerated `dark-scheme-golden.json` (an unrelated prior pipeline's fixture, legitimately stale after the intentional platform-token change). Self-implemented/self-verified — flagged as a follow-up above, not silently absorbed. — `docs/work-log/2026-08-27-presbyportal-brand-kit.md`

- [x] 2026-08-27 — **Platform home, merged post-login landing, and the platform portal** — SHIP WITH NOTES. `/` redesigned as a real marketing page (connectional-courts hero, a new documented `hero` type-scale exception); `/home` absorbs `/orgs` (org cards + Platform block — Admin/Developer cards independently gated per DECISION-044 — + what's-new + feedback prompt), `/orgs` deleted in favor of a permanent 308 redirect, `destination.ts`'s chooser target changed by one literal (fast paths unchanged, Reading A); `/admin` redesigned as a domain-sectioned, permission-filtered tile grid mirroring the org portal's own design language (`src/lib/admin-portal/tiles.ts`, DECISION-123) — fixing two real defects found along the way (all-ten-cards-shown regardless of held permission; `FEATURES.ADMIN_TICKETS` had no tile). `platform.merged_home` ships seeded ON (a live ship, not dark). Shared `TileGrid`/`DomainTileSections` genericized across both axes in the same commit (DECISION-125). Project named **PresbyPortal** mid-pipeline (DECISION-126), reflected in the new landing copy. — `docs/work-log/2026-08-27-platform-home-and-portal.md`

- [x] 2026-08-27 — **Presbytery Increments 3 + 3b — congregation oversight + statistics** — SHIP WITH NOTES. `congregation_oversight` (viability score, buildings/insurance — presbytery-owned judgment about any member congregation, managed or not, no publication dependency; this is how "Q1" dissolved — it was never a downward-read problem) and `congregation_statistics` (SASR-shaped aggregates, a `provenance` column coalesced at read time — `presbytery_entered`/`published_by_congregation`/`imported`; publication wins per congregation+year, presbytery-entered rows never deleted) at `/o/<slug>/admin/oversight` and `/admin/reports`, replacing the portal-IA coming-soon stubs on the same flags (now seeded off, the production convention). `per_capita_rates`/`per_capita_records` derive billed amounts from the coalesce at a presbytery-set `basisYear` (defaults billing-year-minus-2, the real PC(USA) arrears convention), snapshotting at generation time so a later correction can't retroactively move an issued bill. The platform's first cross-org write (`presby_publish_sasr_snapshot()`, SECURITY DEFINER, target derived via `parentId` — never caller-supplied) ships in this migration; its congregation-side UI is Increment 4a, tracked separately. Freeze trigger + `supersedesPublicationId` chain for corrections. Four new permissions. The `(public)/site/<slug>` presbytery/synod/GA fallback (DECISION-121) ships alongside. Known gap: new worshiping communities excluded from all three surfaces — tracked above. Test-RLS §29 (32 checkpoints), 137 tests. — `docs/work-log/2026-08-27-presbytery-oversight-statistics.md`, `docs/work-log/2026-08-27-presbytery-program.md`

- [x] 2026-08-27 — **Presbytery functionality Increments 0 + 2 — verified portal + ministerial credentials** — SHIP WITH NOTES. Increment 0: the generic org portal verified end-to-end for a presbytery org (four congregation-assuming copy fixes shipped; congregation-shaped surfaces like the officers office list honestly left for their own increments, not papered over). Increment 2: `/o/<slug>/admin/credentials` — record ordinations, change standing vs. end ordination (two UI controls, one gated `changeOrdinationStatus` write path; status changes never touch `endedOn`), record/end pastoral appointments in a new presbytery-owned `appointments` table (composite FK + parent-path serving-org validation, enumeration-safe). New `ordinations.status`, `credentials.manage` permission, `presbytery_stated_clerk` template (first non-null `organization_type_scope`; congregation exclusion regression-tested after a QA loop-back added the missing scope test), four audit keys, migration 0037, RLS §28 17/17. `org_portal.credentials` seeded off. Increments 1/3/4/5 tracked above. — `docs/work-log/2026-08-26-presbytery-functionality.md`

- [x] 2026-08-27 — **Events model Increments 1–2 — schema + admin CRUD** — SHIP WITH NOTES. Flat, composite-keyed, FORCE-RLS `events` table (migration 0036) carrying the frozen six-column check-in contract (DECISION-113, verified byte-for-byte at QA) with materialized recurrence (`parent_event_id` + pattern/count, 52-occurrence series-total cap — DECISION-115; pattern edits extend forward, never rewrite — enforced structurally by disjoint write paths). Admin surface at `/o/<slug>/admin/events`: create single/series, edit, soft-cancel (no delete), extend series. New `events.manage` permission with no default role binding; `org_portal.events` flag seeded off. `is_public` present but inert until Increment 4 (public calendar) — note that inertness in the Increment 4 kickoff. Unblocks children's-ministry Increment C (tracked above). — `docs/work-log/2026-08-26-events-model.md`

- [x] 2026-08-27 — **Children's ministry Increment A — roster + guardian links** — SHIP WITH NOTES. New children's roster (`/o/<slug>/admin/members/children`, age computed at read time from `dateOfBirth`, no-DOB rows excluded with the caveat disclosed in the empty state) and staff-entered guardian/emergency-contact links (`/admin/members/<id>/edit/guardians`, existing-person typeahead first, free-text fallback, XOR-validated server-side). New tier-2 `children.roster` permission on a new `children_ministry_admin` role, deliberately separate from `member_care_admin` (DECISION-111); first-ever application-level gating on `person_relationships`, with the F21-shaped existence-oracle narrowed at the app layer; unmasked-DOB scope disclosed in the permission's own catalog description (DECISION-114, Phase 6 finding closed at commit). Ships behind `org_portal.children_ministry` (seeded off). Follow-ups (persistent missing-DOB notice, guardian-search empty message, Increments B/C) tracked above. — `docs/work-log/2026-08-26-childrens-ministry.md`

- [x] 2026-08-26 — **Portal UX-fixes batch ("Do all of them")** — SHIP IT. All 16 findings from the same-day operator-requested portal UX review (`docs/reviews/2026-08-26-portal-ux.md`) resolved: 15 implemented across four waves, L4 (feedback history) honestly deferred as feature-class. Highlights: a reusable unsaved-changes guard (`useUnsavedChangesGuard` + shared `AlertDialog`, six admin forms, intercepts even shell-rendered Back links, never swallows cmd-click/new-tab), edit-person restructured into three `bg-card` panels, chevrons + a Restricted lock-badge on the sensitive-info page, a CSS-only right-edge scroll cue on the shared Table primitive, distinct Groups/Branding tile icons, directory card chevrons, an overlay mobile nav (no more content push-down), the Parishes dead-end cards demoted to honest info panels, required-field markers, a filter for the racial/ethnic checklist, a styled logo file input, and a lock badge on the tickets denied state. — `docs/work-log/2026-08-26-portal-ux-fixes.md`

- [x] 2026-08-26 — **Portal nav highlighted "Administration" alongside whatever operate-category tile the viewer was actually on** — SHIP IT. Found live: on `/o/<slug>/admin/groups` (and, latently, `.../admin/members`/`.../admin/officers`), both that tile's own nav link AND "Administration" lit up together, because "Administration"'s prefix match (`/o/<slug>/admin`) is a literal string-prefix of every "operate"-category tile that happens to route through `/admin/*`, even though those tiles aren't Organization Administration hub destinations. Fixed by resolving nav active-state to the single most-specific (longest-href) matching entry, not "every entry whose href happens to be a pathname prefix" — the same algorithm most routers use for nested-route active state. No per-tile special-casing needed. Two new regression tests. — `docs/work-log/2026-08-26-admin-flags-table-overflow.md` (bundled, same investigation)
- [x] 2026-08-26 — **`/admin/flags`'s Description column pushed the Toggle button off-screen, requiring an undiscoverable page-level horizontal scroll** — SHIP IT. Reported as "flags aren't clickable... click selects the button text." Root cause: the Description cell had no wrap override, so today's 20+ new, deliberately-long flag descriptions forced the whole table (and, since nothing constrained its container's width, the whole page) to ~1700px wide — clicking where "Toggle" used to sit (before the catalog grew) instead landed on the now-much-longer Description text of an unrelated row, which is ordinary selectable prose. Fixed by wrapping the Description column instead of forcing it onto one line; table now fits the viewport with no scroll needed. — `docs/work-log/2026-08-26-admin-flags-table-overflow.md`
- [x] 2026-08-26 — **Groups and committee administration** — SHIP WITH NOTES. New `/o/<slug>/admin/groups` (gated `org_portal.groups`, seeded off): create/edit managed groups (committee/small_group/choir/team), add/end members with a role and dates. Session/Board of Deacons/Active Membership stay untouched — a derived group's id 404s at the route layer, and two invariant-hardening trigger fixes (DECISION-110) now make the database itself reject a direct edit or membership-delete on a derived group, closing a real "trigger"-class gap that had silently degraded to "paper." Also extends `seedGroupTypes()` to seed the platform-wide `committee`/`small_group`/`choir`/`team` templates production was missing. Found and fixed live: `group_types` platform rows being invisible to the tenant RLS connection (now read via `getPlatformDb()`), and the cross-pipeline test-teardown regression logged separately below. Follow-up: what's-new advisory deferred until the flag is flipped on for a real org. — `docs/work-log/2026-08-26-groups-admin.md`
- [x] 2026-08-26 — **Groups list now shows the org's derived groups (Session, Board of Deacons, Active Membership) as a read-only "Automatic rosters" section** — operator-requested same-day follow-up ("should groups show the dynamic groups? ... i think it should"). New `listDerivedGroups()` export in `src/lib/groups.ts`, gated by the same `groups.manage`/`hasGroupsManage` check as every other export; zero new write paths — `updateGroup`/`addGroupMember`/`endGroupMembership` still independently scope to `membership_source = 'managed'`, unchanged. Session/diaconate rows link to `/o/<slug>/admin/officers`; Active Membership shows a short note instead, since it has no management surface of its own. Phase 2 (architect) skipped per Classification (read-surface addition, no schema/deps/structural change). Live-verified at 360px against fpcw's real seeded data. — `docs/work-log/2026-08-26-groups-show-derived.md`
- [x] 2026-08-26 — **Cross-pipeline test-teardown hazard: `group_memberships_reject_derived`'s new DELETE-rejection (DECISION-110) breaking every DB-backed test file's `afterAll`** — fixed, no work-log (Trivial/mechanical harness fix, per Classification). 13 test files whose `afterAll` cascades `platform.delete(organizations)` through a fixture-created active_membership/session/diaconate derived group now wrap that delete with `alter table group_memberships disable trigger group_memberships_reject_derived` / re-enable, matching `src/lib/roll.test.ts`'s existing pattern for `roll_actions_freeze`: `people-update.test.ts`, `people.test.ts`, `role-definitions.test.ts`, `role-grants.test.ts` (missed by an initial single-line grep — its `.delete(organizations)` call was split across lines), `officers.test.ts`, `directory.test.ts` (two independent `afterAll` blocks), `org-features.test.ts`, `tenant-branding.test.ts`, `tickets.test.ts`, `sites.test.ts` (two blocks), `brand/read-org-brand.test.ts`, `org-portal/home-data.test.ts`, `org-portal/find-person.test.ts`. `groups.test.ts` (today's own groups-admin pipeline) and `person-sensitive.test.ts` (fixed separately) were already correct or handled elsewhere. All 14 fixed `afterAll` blocks re-run individually against the real dev DB and complete cleanly, teardown included. Removed 7 orphaned pre-fix fixture organizations (`role-definitions`/`role-grants` stamp `1787787015841`/`1787787015843`) and their 2 stamped-email fixture users from the shared dev database; no `people` rows were removed (names aren't stamp-unique, left as low-risk debris per the conservative instruction). `npm run typecheck` and `npm run check` (all four tripwires) both clean. — reported 2026-08-26, root cause in `docs/work-log/2026-08-26-groups-admin.md`

  **Correction, 2026-08-26 (QA Phase 5 FAIL loop-back):** the above pass missed `src/lib/roll.test.ts` itself — the very file whose pre-existing `roll_actions_freeze` disable/re-enable wrap was used as the template for the other 13 fixes. Its own trigger-disable presence caused the original grep-based "does this file already have a disable-trigger pattern" check to wrongly mark it as already-fixed, when it needed the *new* `group_memberships_reject_derived` trigger disabled too, not just the pre-existing one. Fixed: added a second, independent disable/enable block around `roll.test.ts`'s `platform.delete(organizations)` call, same syntax as the other 14 files. Re-audited every DB-backed test file that creates a `membershipSource: "derived"`/officer-terms-derived fixture AND cascades an organization delete in teardown (checked actual trigger coverage per file, not list membership) — `org-provisioning.test.ts`, `src/app/api/sites/ingest/route.test.ts`, and `src/lib/storage/blob-store.test.ts` were also candidates by a broader grep but confirmed NOT affected (none of them ever populate a `group_memberships` row for a derived group — no `memberships.currentRoll`/`officer_terms` insert in their fixtures, so the cascade never touches a row the trigger guards). No other gaps found. `dotenv -e .env.local -- npx vitest run src/lib/roll.test.ts` now passes 1/1 file, 20/20 tests, clean teardown, run twice. `npm run typecheck`/`npm run check` clean. Cleaned up 12 orphaned `people` rows + 6 orphaned `users` rows left in the shared dev database from `roll.test.ts`'s pre-fix failed teardown runs earlier today (2 stamps); confirmed zero `roll-test`-prefixed orgs/users remain. — `docs/work-log/2026-08-26-groups-admin.md` Phase 4 addendum
- [x] 2026-08-26 — **Tiered sensitive information for members** — SHIP WITH NOTES. New sub-screen (`/o/<slug>/admin/members/<id>/edit/sensitive`, gated `org_portal.sensitive_info`) exposes four fixed tier-3 tables (pastoral notes, demographics, medical, disabilities) that have existed in the schema since day one but never had a UI. Four independently-gated permissions, no role holds more than two, bound per DECISION-078's constitutional-duty test (`pastoral_notes.manage` → `installed_pastor`, `demographics.manage` → `stated_clerk`, `medical.manage`/`disabilities.manage` → new `member_care_admin` role). Enumeration-safe (permission check runs before any person lookup), `clergy_only` notes omitted for non-clergy holders, fixed fields only (D8 — no custom-field builder). Retires the orphaned `pastoral.notes.view` permission. Follow-ups: server-side length validation (client-only shipped, not server), a 360px browser check, all tracked below. — `docs/work-log/2026-08-26-member-sensitive-info.md`
- [x] 2026-08-26 — **Server-side length enforcement added to the sensitive-info write functions** — Phase 6 follow-up closed. `addPersonNote` (`body`, 4000), `setPersonDemographics` (`gender`, 2000), and `setPersonMedical` (`allergies`/`medicalNotes`/`medications`/`authorizedPickup`, 4000 each) in `src/lib/person-sensitive.ts` now reject an over-limit field with a typed `{kind: "invalid_input", field}` result — same numbers as `sensitive-info-form.tsx`'s pre-existing client-side `maxLength`, checked inline before `withOrgContext()`, matching `roll.ts`'s `recordRollAction()` pre-check convention. `sensitive/actions.ts` maps the new variant to a human-readable, field-naming toast message. 6 new real-Postgres tests (within-limit ok, over-limit invalid_input + no write) plus 3 new mocked orchestration tests. `person_disabilities.category` has no free-text client input to match (checkbox-only), so it was left unchanged. `npm run typecheck`/`npm test`/`npm run check` all clean. — `docs/work-log/2026-08-26-member-sensitive-info.md` Phase 4 addendum
- [x] 2026-08-26 — **Role definition administration** — SHIP WITH NOTES. New "Role catalog" section on `/o/<slug>/admin/roles` lets a `roles.manage` holder create a custom role, choose its permissions (tier-grouped, tier-3 boundary always visible), edit an existing custom role's permission set, adopt a stock template, or deactivate a role (soft — ends live grants atomically, never a hard delete). New `roles.manage` permission (distinct from `role_grants.manage`) bound to a new `role_admin` role, not `stated_clerk` (DECISION-106/109). Closes a real escalation gap `grantRole()`'s own subset check didn't cover: a shared `assertPermissionSubset()` (delta-only on edit, full-set on create/grant) plus a definition-side self-lockout guard, both adversarially test-covered against real Postgres/RLS. Ships behind the existing `org_portal.roles` flag. Follow-ups: release-notes entry, functionality-map correction, and a cascade-FK risk on `role_grants.roleId`, all tracked. — `docs/work-log/2026-08-26-role-permissions-admin.md`
- [x] 2026-08-26 — **Record a roll action for an existing member from the Edit screen** — SHIP WITH NOTES. New "Record roll action" section on `/o/<slug>/admin/members/<id>/edit`, gated on the existing `roll.propose` permission (deliberately not `people.manage`, DECISION-107), ships behind a new dedicated `org_portal.members_roll_action_edit` flag (seeded off). Offers only roll-*gain* kinds (profession of faith, reaffirmation, restoration, certificate received, enrollment onto baptized/affiliate/other-participant rolls) — death/transfer-out/removal are intentionally excluded this increment because F19's termination trigger doesn't actually exist (schema-design.md corrected from "Applied §8" to "Open" the same day). Warns rather than blocks on an already-pending action for the same person. No audit event (roll-action proposal is deliberately unaudited, matching existing precedent). Follow-ups: F19 trigger, a real `createPerson()` roll-cache bug found along the way, and a 360px live-browser check, all separately tracked below. — `docs/work-log/2026-08-26-member-roll-on-edit.md`
- [x] 2026-08-26 — **Retrofit `appearance-none` + chevron onto the rest of the app's native `<select>`s** — SHIP IT. Every remaining native `<select>` missing the fix `directory-grid.tsx`/`admin/members/members-list.tsx` got earlier the same day now carries `appearance-none` + a `pointer-events-none` `ChevronDown` in a `relative` wrapper: `(org)` tickets/branding/officers/roles/member-wizard/edit-person forms, `(admin)` organization-create/service-times/brand forms, all five per-row ticket controls plus the tickets queue filters, `feedback-status-control.tsx`, `admin/audit/page.tsx`, `admin/users/page.tsx`, and `shared/feedback-form.tsx`. Pure CSS retrofit, no schema/API/permission change. — `docs/work-log/2026-08-26-select-appearance-fix.md`

- [x] 2026-08-26 — **Tenant-facing branding management** — SHIP IT. Congregations can now set their own seed color, type pairing, logo, and light-only toggle at `/o/<slug>/admin/branding` (new tile in the org-admin hub), instead of branding being platform-admin-only. New `branding.manage` permission bound to a new `brand_admin` role — not `stated_clerk`, per DECISION-101's constitutional-duty ruling and the operator's own confirmation that branding belongs to the organization. `withOrgContext()`-only, no schema change (`organization_brands`/`organization_brand_history` were already provisioned for this in `drizzle/0016`). New `TENANT_BRAND_SET` audit key, distinct from the platform's `ORG_BRAND_SET`. "Neutralize" and the platform admin's own brand form both stay live, unchanged, as an override/break-glass path. Fixes a known `BrandForm` staleness bug (found while adapting the form) rather than reproducing it. — `docs/work-log/2026-08-26-tenant-branding-permission.md`

- [x] 2026-08-26 — **Portal visual modernization** — SHIP WITH NOTES. Bold `bg-primary` hero band on the portal-home greeting, a new `Button` `tile` variant for the tool tiles, two `TYPE_SCALE` corrections (tile description + footer address/phone moved `dense`→`body`), an unconditional `border-primary`/`border-transparent` nav-active accent, and one CSS-only mount fade-in on the greeting gated behind a new `org_portal.motion` flag (seeded off) — all routed through the existing brand-token cascade, no new color tokens, no new dependency. `prefers-reduced-motion` confirmed live to neutralize the animation. Same-day operator correction (DECISION-105): the tile's original full-bleed solid fill read as flat, replaced with a `bg-card` + border + hover-lift-shadow card, brand color moved to a small icon badge, corners rounded (`rounded-xl`) — which also closed the previously-open zero-vertical-padding gap as a side effect. — `docs/work-log/2026-08-26-portal-visual-modernization.md`

- [x] 2026-08-26 — **Organization Administration hub** — SHIP WITH NOTES. New `/o/<slug>/admin` index page (gated `org_portal.admin_hub`, seeded off), reached from a new flag-gated "Administration" entry in the persistent portal nav. `PORTAL_TILES` gained a `category: "operate" | "administer"` discriminant (pure routing, never a permission gate — DECISION-003, verified independently by qa and Phase 6 via direct source read + automated test); `roles`/`features`/`branding` are `administer`, `directory`/`members`/`officers`/`feedback` are `operate`, and `tickets` is `administer` (DECISION-105's same-day correction — members/officers are routine congregational work, filing a support ticket is closer to org setup). The already-shipped `/o/<slug>/admin/features` page is now discoverable for the first time. No new permission, no schema change. — `docs/work-log/2026-08-26-portal-reorg-and-modernization.md`

- [x] 2026-08-26 — **Portal papercuts: officers nav visibility, sign-out destination, directory button group, wider shell** — SHIP IT. Officers tile added to `PORTAL_TILES` (closes the reachability gap the same day's officer-terms Phase 6 named). Sign-out (`signOutAction`) now takes a caller-bound redirect target — `(org)/o/[slug]/layout.tsx` sends it to the org's own public site (`/site/<slug>`) instead of the generic platform home, which also closes the loop back into the already-working branded `/signin`. New dependency-free `src/components/shared/button-group.tsx`, applied to the directory's Members/Households/Parishes switcher with icons. `(org)`'s shell widened `max-w-4xl` → `max-w-6xl`. Also, investigated live and fixed: `/signin`'s org logo was boxed (`OrgMark`'s fixed G7 plate, meant for a compact header, not a page's own identity) — swapped for the unboxed `OrgWordmark` (matching the portal header's own treatment) linked back to the public site; the reported "weird" Google-button color was confirmed correct and intentional (platform-pinned, anti-phishing), no change made. `e2e/branded-signin.spec.ts` (CLAUDE.md's mandatory auth-touching smoke) re-run 4/4 green against a real dev server. — `docs/work-log/2026-08-26-portal-papercuts.md`
- [x] 2026-08-26 — **Officer-terms administration** — SHIP IT. `officers.manage` permission (bound to the existing `stated_clerk` role, no new role minted — `clerk_of_session` stays a data value on `officer_terms.office`, never a role key, keeping the ecclesiastical office and the software grant deliberately unlinked). `src/lib/officers.ts` (record/end a term, roster, per-person history), `/o/<slug>/admin/officers` + `/officers/[personId]`, no-delete UI (only start/end), F22 application-layer regression test proven against real Postgres, mobile-verified live at 360px (found and fixed a real overflow). Ships behind `org_portal.officers` (seeded off). — `docs/work-log/2026-08-26-groups-and-officers.md`
- [x] 2026-08-26 — **Portal card hover treatment, icons, and a footer (v3)** — SHIP IT. Adopted `fpcw-directory`'s shadow-lift card treatment (withheld `cursor-pointer` on multi-link cards, per DECISION-099) and icon set onto the org portal's tiles/directory cards, and shipped the portal's first footer (org contact info + nav recap + copyright, via a new RLS-verified `getOrgProfileForFooter()` reader), all behind a new `org_portal.chrome_v3` flag (seeded off, independent of the already-live `chrome_v2`). Nav dropdowns were explicitly scoped out this pass, not built. Release notes: `docs/release-notes/v0.15.md`. — `docs/work-log/2026-08-26-portal-fpcw-directory-ux.md`
- [x] 2026-08-26 — **Org provisioning seeds derived groups AND a baseline `member`/`directory.view` role (F16 closed for that half).** `createOrganization()` now seeds a constitutional `member` `app_roles` row bound to `directory.view`, granted via `role_grants`' group arm to the org's own `active_membership` group, in the same transaction as the F16 group seed (`baselineRoleSeedPlan()`, sibling to `groupSeedPlan()`). The founding-administrator (`stated_clerk`-equivalent) bootstrap half is explicitly NOT closed here — it depends on person/membership creation, which has no in-app path today, and is tracked under the queued P2 (backbone and onboarding) pipeline in `docs/STATE.md`, not as a standalone line here (DECISION-100). — `docs/work-log/2026-08-26-org-provisioning-baseline-roles.md`
- [x] 2026-08-26 — **`identity.dateOfBirth` wizard Next→Back data-loss race — fixed with a real-timing regression test.** Root cause: a native `<input type="date">`'s change-commit can land a macrotask after a rapid tap on Next, and `form.trigger()` running unwaited would already have read the stale (empty) value and moved the step away, unmounting the field before the late-arriving commit landed. Fix: `handleIdentityNext` now blurs the active element and waits one animation frame before validating, giving a deferred native commit time to land. Regression test in `member-wizard.test.tsx` fires the native `change` event ~5ms after Next is tapped (a real macrotask gap, not jsdom's default synchronous ordering) — confirmed failing red against the pre-fix code, green after. — `docs/work-log/2026-08-25-member-management.md` Phase 6
- [x] 2026-08-26 — **No fixture role held `roll.approve`** — bound directly to `stated_clerk` (Tobias Renwick, `f0000000-…-0005`) in `scripts/seed-dev.sql`, mirroring the `roll.propose`/`people.manage`/`org_features.manage` bindings already there; applied live to the dev database. The one office now holds every permission Increment 1's folded propose+approve wizard needs. — `docs/work-log/2026-08-25-member-management.md` Phase 5 QA
- [x] 2026-08-26 — **Dev-DB hygiene: leftover `organization_feature_toggles` row** — the stray Alder Creek row deleted from the dev database, and `scripts/test-rls.sql` section 19's first INSERT changed to `ON CONFLICT (organization_id, feature_key) DO UPDATE` (the durable fix QA recommended, not just a one-time cleanup) — verified section 19 now runs clean twice in a row in isolation. — `docs/work-log/2026-08-25-member-management.md` Phase 5 QA
- [x] **SECURITY — credentials sign-in 2FA bypass via Server-Action redirect inlining** — FIXED 2026-08-24 (SHIP IT, full bug-fix pipeline: `signIn(..., {redirect:false})` + server-side `computeEffectiveTwoFactor()` gate in `signInWithCredentials`, fail-closed on a row miss; network-trace regression `e2e/totp-callback-bypass.spec.ts` 4/4, red→green independently re-verified by qa). Residual `PROTECTION_RULES` inlining variant tracked separately in Next Up. Awaiting commit. — `docs/work-log/2026-08-24-totp-callback-bypass-fix.md`

- [x] 2026-08-24 — **Admin: create organization** — the one missing write path for `organizations` (previously no route/action/UI anywhere could create a new org row — every existing one came from raw SQL). Discovered mid-task while onboarding the first real congregation (First Presbyterian Church of Westerville, slug `fpcw`). `src/lib/org-provisioning.ts` (`createOrganization()`, `deriveOrgPath()`), `src/lib/reserved-slugs.ts`, `/admin/organizations/new`, F16 derived-group seeding (session/diaconate/active_membership, conditional on org type) folded into the same transaction so officer-term recording works day one, `AUDIT_ACTIONS.ORG_CREATED`. SHIP WITH NOTES — `docs/work-log/2026-08-24-admin-org-create.md`
- [x] 2026-08-20 — `blob-store.test.ts`'s two oversized-payload assertions updated to the 10MB bound (DECISION-071/073 widened it from 2MB, but the tests weren't updated to match, so they silently passed for the wrong reason). Found by role-catalog's Phase 5 qa run re-running DB-gated suites rather than accepting a skip; fixed same-day — `docs/work-log/2026-08-20-role-catalog.md` Phase 5
- [x] 2026-08-20 — `recordSiteIngest()` unconditionally wrote `organization_sites.status = 'live'` on every ingest, including over a prior admin-set `'suspended'` — an admin suspending an abusive tenant's site was undone by the very next routine CI push, with no gate at all. The implementer flagged this loudly rather than silently shipping it; the orchestrator fixed it before commit (status now preserves `'suspended'` across ingest, still promotes `'provisioning' → 'live'` on first success, bundle/commit metadata still advances regardless), with a new regression test — `docs/work-log/2026-08-20-public-sites.md` Phase 4 commit 2

- [x] 2026-08-19 — **`role_grants`' arm-1 (`person_id`) cascade-on-membership-end gap: closed at the DB level for the ordinary mutation path, not merely tracked.** `drizzle/0014_presby_org_router.sql`'s `presby_guard_membership_end()` trigger (predating both DECISION-062 and DECISION-066 by migration number) already rejects the state; `scripts/test-rls.sql` lines 471–485 already assert it. DECISION-062/066 both corrected with an appended note. `listGrants()`'s `membershipEnded` field is retained as correct-but-currently-unreachable defensive code for direct historical-data imports only — `docs/work-log/2026-08-19-tenant-administration.md` Phase 4 commit 2 / Phase 6

- [x] 2026-08-19 — `AUDIT_ACTIONS.TENANT_ROLE_GRANTED`/`TENANT_ROLE_REVOKED` added and wired: `src/lib/audit.ts`, written from `src/app/(org)/o/[slug]/admin/roles/actions.ts` on every successful grant/revoke — DECISION-062 (P1 Phase 2) — `docs/work-log/2026-08-19-tenant-administration.md` Phase 4 commit 2
- [x] 2026-08-19 — P0.5-slice-B carry-overs from P0 (radix-ui umbrella, radius remap, `alert-dialog.tsx` regeneration, `ui-standards.md` rewrite) all resolved in the brand-foundation pipeline (`ui:add`/`check:deps-drift`, DECISION-048, commit `a4`, commit `0.2`) — confirmed by Phase 6 — `docs/work-log/2026-08-19-brand-foundation.md`
- [x] 2026-08-19 — next-auth `5.0.0-beta.31` → `beta.32`: clears 2 critical + 1 high Auth.js advisories (`@auth/core` `0.41.3`); mandatory auth e2e gate PASS (89/89 incl. `totp-full-login.spec.ts` run explicitly); CI audit step's `continue-on-error` removed and confirmed passing clean — `docs/work-log/2026-08-19-nextauth-beta32.md`
- [x] 2026-08-19 — **P0.5 slices 0 and a — SHIP WITH NOTES.** Ten commits (`0.1`, `0.2`, `a1`–`a8`) plus the `/admin/design-system` sign-off page: the closed three-way token contract, AAA contrast throughout, class-driven dark mode, five new primitives, a real alert-dialog, the full admin/credential/member sweep onto primitives, and `check:brand-scope`'s C2 rule live tree-wide (adversarial canary confirmed three separate times — a8, Phase 5, Phase 6). QA's independent Phase 5 (re-run from clean, 89/89 e2e, live C2 canary, independent visual parity) and the analyst's Phase 6 both closed DECISION-045's deferral for this scope — `docs/work-log/2026-08-19-brand-foundation.md`
- [x] 2026-08-19 — Dark `--destructive` ruling resolved: the "move the text, not the hue" alternative shipped in `f0ebd7c` (fill stays `hsl(0 84% 60%)`, foreground → page ink; 4.73:1 on the pair, rendered `text-destructive` retained at 4.73/3.92). Stale prose describing the rejected 48%-plus-white correction fixed in `globals.css` and work-log note 7 — `docs/work-log/2026-08-19-brand-foundation.md` Phase 4 commit `0.1`
- [x] 2026-08-19 — Avatar menu + organization switcher: identity and context split on Google's model; caught a Radix bug that would have silently broken sign-out — `docs/work-log/2026-08-19-avatar-and-org-switcher.md`
- [x] 2026-08-19 — v0.10.0 release notes + functionality map + version bump
- [x] 2026-08-18 — P0 post-login router

- [x] 2026-08-18 — Stale test replica retired: `(auth)/totp/actions.test.ts` deleted with the `/home` → `/launch` fallback change; its javascript:/data:/protocol-relative assertions now live in `src/lib/auth/safe-callback.test.ts` against the real function — `docs/work-log/2026-08-18-backbone-and-org-sites.md` (P0 slice C1)
- [x] 2026-08-18 — Agent tool grants: `tools:` on all nine agents; analyst/architect/qa read-only and return their section for the orchestrator to record; QA re-chartered verification-only (test authorship → implementers); stale e2e briefing in `qa.md` corrected — SHIP IT — `docs/work-log/2026-08-18-agent-tool-restrictions.md`
- [x] 2026-08-18 — 2FA: duplicate self-enrolment surface at `/admin/2fa` replaced by a per-congregation policy page; `organization_settings.require_two_factor` + `presby_two_factor_required()` (SECURITY DEFINER, F26); M5 closed by deletion; dead `prepareEnrollment` removed; TOTP config crash made graceful — 39 RLS assertions, 434 unit, 49 e2e — SHIP IT — `docs/work-log/2026-08-18-two-factor-policy.md`
- [x] 2026-08-18 — E2E owns its test users (DECISION-032): fixtures hardcoded in `e2e/support/users.ts` and provisioned by globalSetup; all `SEED_*` vars and every `test.skip()` credential guard deleted; **48/48 now run with zero configuration** (was 6 passed / 42 silently skipped) — SHIP IT — `docs/work-log/2026-08-18-e2e-owns-its-users.md`
- [x] 2026-08-18 — Deck removed (DECISION-031, supersedes D005): `deck/` deleted with its four npm scripts, the CLAUDE.md re-render rule, and the `/pre-push` staleness check — SHIP IT — `docs/work-log/2026-08-18-remove-deck.md`
- [x] 2026-08-18 — Identity pass: README rewritten for presby; landing page, global nav, admin shell, email From, and TOTP issuer de-startered; `.env.example` gained the three missing required connection strings + `DEV_ALLOWED_ORIGINS`; 9 agent files re-pointed — SHIP IT — `docs/work-log/2026-08-18-identity-pass.md`
- [x] 2026-08-18 — Stale test guards repaired: deleted the obsolete neon-http `db.transaction()` guard (moot since the F28 driver switch) and re-pointed the cron-maintenance assertion at the F29 roll reconcile instead of a bare call count; suite green again at 424 — SHIP IT — `docs/work-log/2026-08-18-stale-test-guards.md`
- [x] 2026-08-09 — Enforcement batch from the Fable external review: CI runs both tripwires + `npm audit` + commit-grammar job; dependabot; e2e-in-CI on ephemeral Neon branch + playwright `webServer`; cadence-check SessionStart hook; pre-push PreToolUse gate (Rule 5 mechanized); `Work-Log:` commit trailer (required on feat/fix); `AGENTS.md` shim; opt-in Claude PR review — `docs/work-log/2026-08-09-enforcement-batch.md`
- [x] 2026-07-13 — PR #3 closed as superseded (its skill copy was the pre-generalization fork variant; main's port from 20fb316 stands); contribution kit landed as `docs/starter-contributions/README.md` with a live-status banner (stale "dormant" caveat + top-down PR order removed); fork-sync skills + kit added to the functionality map
- [x] 2026-07-12 — Functionality map harvested from huddleup.health: `docs/product/functionality-map.md` + SessionStart index hook + Workflow Rule 14 + release-notes/personalize-starter wiring — `docs/work-log/2026-07-12-functionality-map.md`
- [x] 2026-07-11 — Instruction-layer slim: CLAUDE.md 451→~330 lines, 9 agents de-duplicated + de-drifted, handoff format single-sourced to work-log template, review cadences consolidated to two slots (DECISION-029) — `docs/work-log/2026-07-11-instruction-layer-slim.md`
- [x] 2026-07-02 — v0.6.0 shipped: PR #4 merged to main (30 commits — member home, feedback dev-loop, admin ops suite, email reliability, security batch, 6 fixes, tooling); branch deleted via /merge-pr
- [x] 2026-07-01 — `/test` + `/test-results` skills ported (commit 4afcc36)

- [x] 2026-07-02 — E2E feedback test-data cleanup — globalSetup preflight sweep scoped to seed-user emails; 9 stale rows swept on first run — SHIP IT — `docs/work-log/2026-07-02-e2e-feedback-cleanup.md`
- [x] 2026-07-02 — `(email-verify)` error boundary — `error.tsx` added at group level; ui-standards.md rule written — SHIP IT — `docs/work-log/2026-07-02-email-verify-error-boundary.md`
- [x] 2026-07-02 — TOTP enrollment-stranding fix — two-hop redirect chain (proxy → /totp → /account/2fa?callbackUrl=) implemented; "Continue" CTA; auth e2e gate cleared 48/48 — SHIP IT — `docs/work-log/2026-07-02-totp-enrollment-redirect.md`
- [x] 2026-07-02 — Admin lock-state visibility + unlock — amber badge + inline unlock form; `USER_ACCOUNT_UNLOCKED` audit event; check:audit passes — SHIP IT — `docs/work-log/2026-07-02-admin-lock-visibility.md`
- [x] 2026-07-02 — Turnstile CAPTCHA (no-op until keyed) — endpoint-level verification in authorize(); fail-open; CSP updated; 48/48 e2e with no keys — SHIP IT — `docs/work-log/2026-07-02-turnstile-captcha.md`
- [x] 2026-07-02 — Email queue admin viewer + Resend delivery webhook — migration 0006; hand-rolled HMAC (3 Phase 2 mandates); /admin/email-queue viewer + retry; 48/48 e2e — SHIP IT — `docs/work-log/2026-07-02-email-observability.md`
- [x] 2026-07-02 — Member-visible what's-new — migration 0007; validateWhatsNewEntry (HTML-reject, spread emoji check); admin CRUD; member /home card + /whats-new; Rule 13 advisory; 48/48 e2e — SHIP IT — `docs/work-log/2026-07-02-whats-new.md`
- [x] 2026-07-02 — Audit log viewer at `/admin/audit` — pure RSC; input guards with 13 unit tests; grouped action select; admin.audit permission; SOC 2 fork note; 48/48 e2e — SHIP IT — `docs/work-log/2026-07-02-audit-log-viewer.md`
- [x] 2026-07-01 — Process batch: `docs/ui-standards.md`, `downstream-sync` skill, QA feature-gate table + no-self-agreeing-mocks, force-push guardrail (Rule 11 + deployment-engineer), `/pre-push` CVE step, database-admin `onDelete` rule, api-developer email-escape rule, TODO ledger (Rule 10) — harvest Tier 3 #16-20, #23 (instructions)
- [x] 2026-07-01 — BUG-1: verify-email `db.transaction()` on neon-http → `db.batch()` — SHIP IT — `docs/work-log/2026-07-01-verify-email-neon-http-transaction.md`
- [x] 2026-07-01 — BUG-2: 2FA fresh-recovery-codes cookie deleted during RSC render — SHIP IT — `docs/work-log/2026-07-01-2fa-fresh-codes-rsc-cookie.md`
- [x] 2026-07-01 — BUG-3: first-time Google OAuth sign-in gets AccessDenied — SHIP IT — `docs/work-log/2026-07-01-oauth-first-signin-accessdenied.md`
- [x] 2026-07-01 — BUG-4: NextAuth `trustHost` unset — OAuth hard-blocked off-Vercel — SHIP IT — `docs/work-log/2026-07-01-nextauth-trusthost.md`
- [x] 2026-07-01 — Post-login routing + member home + global nav + e2e hardening — SHIP IT, committed `18f04a7`
- [x] 2026-07-01 — Sibling harvest of 7 fork repos → classified punch-list — committed `4c54e4f`
- [x] 2026-07-01 — recordAudit() helper (audit ip/user_agent, request-ip.ts shared module, logAttempt elimination) — SHIP IT — `docs/work-log/2026-07-01-record-audit-helper.md`
- [x] 2026-07-01 — isUniqueViolation() helper (verify-email ErrorCard, password-reset atomic upsert) — SHIP IT — `docs/work-log/2026-07-01-unique-violation-helper.md`
- [x] 2026-07-02 — Mobile (360px) visual pass — account+admin sidebar layout, global nav crowding — CSS-only, batch QA covers regression — `docs/work-log/2026-07-02-mobile-360-pass.md`
- [x] 2026-07-01 — E2E auth infra (cached storageState, role boundaries, DB isolation guard) — SHIP IT — `docs/work-log/2026-07-01-e2e-auth-infra.md`
- [x] 2026-07-01 — Durable email queue with retry (persist-first enqueue, cron worker, vercel.json) — SHIP IT — `docs/work-log/2026-07-01-email-queue.md`
- [x] 2026-07-01 — In-app feedback + dev-loop triage wiring — SHIP WITH NOTES (FU-1: e2e test-data hygiene) — `docs/work-log/2026-07-01-feedback-dev-loop.md`
- [x] 2026-07-01 — Report-only CSP + HSTS preload removal — SHIP IT — `docs/work-log/2026-07-01-security-headers.md`
- [x] 2026-07-01 — ACCESS_DENIED audit event on /access-pending bounce — SHIP IT — `docs/work-log/2026-07-01-access-denied-audit.md`
- [x] 2026-07-01 — Per-account login lockout (5-failure → 15-min DB-persisted lock, npvitals bug fix, Gap 2–4 fixes) — SHIP IT — `docs/work-log/2026-07-01-account-lockout.md`
- [x] 2026-07-01 — pendingTaken expiresAt-filter bug (requestEmailChange cross-user collision check) — SHIP IT — `docs/work-log/2026-07-01-pending-email-expiry-filter.md`
- [x] 2026-07-01 — check:sql-date tripwire (catch sql<Date> compile-time lie; umbrella check script; pre-push Step 3c) — SHIP IT — `docs/work-log/2026-07-01-sql-date-tripwire.md`
- [x] 2026-07-01 — Opportunistic expired-token GC (daily /api/cron/maintenance; passwordResetTokens + emailVerificationTokens + userTotpPendingEnrollments) — SHIP IT — `docs/work-log/2026-07-01-token-gc.md`
- [x] 2026-07-01 — cache()-wrap isFlagEnabled + cachedAuth (Scope 1+2; eliminates 2× Tier-A SELECT per request; corrects false framework comment) — SHIP IT — `docs/work-log/2026-07-01-flag-caching.md`
- [x] 2026-07-01 — auth.local_login + auth.require_2fa admin flags (OAuth-only endpoint gate, org 2FA master switch, fail-open postures, e2e green) — SHIP IT — `docs/work-log/2026-07-01-auth-mode-flags.md`

---

**Note (2026-08-27):** the former "presby — In Flight" section (superseded status snapshot from the early build-out) has been retired. Its still-open items were folded into Next Up/Backlog above; its resolved items — Roll UI (shipped and now the basis of most member-management/roll features), Playwright e2e (extensive coverage now exists), `ltree` on `organizations.path` (migrated, see `src/lib/db/domain/org.ts`'s column comment), F16 derived-group + baseline-role seeding (shipped 2026-08-24/26), and "the name" decision (settled — DECISION-126, PresbyPortal) — are simply gone; their history lives in Done above and in `docs/decisions.md`.
