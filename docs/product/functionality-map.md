# Functionality Map

A scannable inventory of everything built, so a session
knows what exists without re-reconning. **Version `0.19.0` · surveyed 2026-08-27.**

This is a MAP, not documentation — one line per capability, with the primary file as
a jump-off point. When it drifts from reality, fix it (Workflow Rule 14). Entries
marked **presby:** are this project's own domain; the rest is the platform shell
inherited from the starter.

---

## Index (short — for the session hook)

- **presby: schema** — 41 domain tables in `src/lib/db/domain/`: organizations (hierarchy, `platform_status`), people + memberships + identifiers, roll actions + transfer certificates, ordinations + officer terms, groups (derived session/diaconate), authorization (permissions, roles, grants, commissions, delegations), privacy/consent/demographics, SASR scaffold.
- **presby: isolation** — `presby_app` NOBYPASSRLS + FORCE RLS on every tenant table; bespoke policies for the global person tables; `withOrgContext()` verifies membership before setting the org GUC; two connections (`db` vs `getPlatformDb()`).
- **presby: authorization** — `presby_effective_permissions(person, org, as_of)` with four arms and provenance; `presby_user_organizations()` for the org list (filters nothing — policy lives in the `userOrganizations` / `availableOrganizations` wrappers); `presby_membership_is_active()` for `withOrgContext`'s pre-context gate; `presby_two_factor_required()` for per-church 2FA at sign-in (all SECURITY DEFINER, F26); `src/lib/authz.ts`.
- **presby: org portal** — `/o/<slug>` tenant-facing pages. **Portal home v2** (gated `org_portal.home_v2`, seeded off): time-aware greeting, find-a-person (server action, single-match jump, `directory.view` re-checked in the action), "Yours" household zone (renders only when non-empty), flag-mirroring tool-tile registry (`src/lib/org-portal/tiles.ts`); flag off → the original stub. **Directory v2** (gated `org_portal.directory` + `org_portal.directory_v2`, seeded off): searchable card grid (name/email/phone ILIKE, shared privacy predicate), households view with zero-visible-member drop, person detail (`/directory/[personId]`) and household detail (`/directory/households/[householdId]`) pages, per-household DeaconCard derived from `officer_terms.org_unit_id` (migration 0025 — a deacon's term carries its district; vacant districts render a neutral state), parishes deacon-roster view + privacy-hidden-row visibility with lock badges for `directory.view_hidden` holders (row-level only; field-level hides stay applied — Phase 6 ratified); flag off → the original flat list (`src/lib/directory.ts`, `src/app/(org)/o/[slug]/directory/`, `docs/work-log/2026-08-24-portal-home-directory.md`). **Portal chrome** (gated `org_portal.chrome_v2`, seeded off): the org's own logo — `OrgWordmark`, the un-cropped icon+name lockup, not the cropped `OrgMark` icon the first cut shipped with, `plate` prop off in this header — replaces the platform wordmark in the header inside `/o/<slug>`, linking to the org's own public site (`/site/<slug>`, not the portal home); `OrgSwitcher` renders the name as screen-reader-only text when a real logo is present (`compact` mode) and stays the sole visible name-bearing element otherwise; plus a persistent, wrapping, flag-filtered menu row (`portal-nav.tsx`, active-state client leaf; feedback entry on its own `org_portal.feedback` flag; `docs/work-log/2026-08-25-portal-chrome.md`, `docs/work-log/2026-08-26-portal-chrome-refinements.md`). A "Members" tile was added to the portal-home tile grid pointing at `/o/<slug>/admin/members`, gated on its own `org_portal.members_create` flag (seeded off). The Edit-person screen (`/o/<slug>/admin/members/<id>/edit`) now also has a "Record roll action" section, gated on the existing `roll.propose` permission (not `people.manage`) behind its own dedicated `org_portal.members_roll_action_edit` flag (seeded off) — offers only roll-*gain* kinds (profession of faith, reaffirmation, restoration, certificate received, enrollment onto baptized/affiliate/other-participant rolls); death/transfer-out/removal are intentionally excluded until a termination trigger exists (`docs/work-log/2026-08-26-member-roll-on-edit.md`). A linked sub-screen (`/o/<slug>/admin/members/<id>/edit/sensitive`, gated `org_portal.sensitive_info`) exposes four fixed tier-3 tables — `person_notes`, `person_demographics`, `person_medical`, `person_disabilities` — that have existed in the schema since the original domain design but had zero UI before this: four independently-gated new permissions (`pastoral_notes.manage` → `installed_pastor`, `demographics.manage` → `stated_clerk`, `medical.manage`/`disabilities.manage` → a new `member_care_admin` role), absent-not-disabled per section, enumeration-safe (a denied viewer and a nonexistent person return an identical result before any person lookup runs), and `person_notes.visibility`'s `clergy_only` rows omitted for non-clergy holders. Fixed fields only — no per-tenant custom-field builder (D8). `src/lib/person-sensitive.ts`, `docs/work-log/2026-08-26-member-sensitive-info.md`. **Groups administration** (`/o/<slug>/admin/groups`, gated `org_portal.groups` + a new `groups.manage` permission): create/edit a `managed` group (committee/small_group/choir/team — the platform-wide `group_types` template is now actually seeded for all four keys, previously only `court`/`roster` existed outside fixtures), add/end members (`group_role`: chair/leader/member, soft-end only). Session/Board of Deacons/Active Membership stay fully out of reach for writes — a derived group's id 404s at the route layer (`getGroup()`'s `invalid_target`) and the database itself now rejects a direct edit or a membership-row delete on any derived group (two invariant-hardening trigger fixes, DECISION-110; "The Court Is Not a Group" moves from `paper` to actually `trigger`-enforced). The same list page also shows the org's three derived groups read-only, in a labeled "Automatic rosters" section below the managed-group table (`listDerivedGroups()`) — session/diaconate rows link to `/o/<slug>/admin/officers`; Active Membership gets an explanatory note instead, since it derives from the roll with no management surface of its own. `src/lib/groups.ts`, `docs/work-log/2026-08-26-groups-admin.md`, `docs/work-log/2026-08-26-groups-show-derived.md`. Also: role administration (`/o/<slug>/admin/roles`, `role_grants.manage` permission, gated `org_portal.roles`) — grant/revoke/view *existing* `role_grants` rows against *existing* `app_roles`, creates neither — support tickets (`/o/<slug>/tickets*`, `/o/<slug>/feedback`, `tickets.file` permission, gated `org_portal.tickets`) — and officer-terms administration (`/o/<slug>/admin/officers` + `/officers/[personId]`, `officers.manage` permission bound to the existing `stated_clerk` role — no new role minted, `clerk_of_session` stays a data value on `officer_terms.office`, never a role key — gated `org_portal.officers`): record/end a session/diaconate term (never delete, matching `group_memberships.officer_term_id`'s unconstrained FK), roster + per-person history, an application-layer regression test proving the write path doesn't reopen F22's bug class. All four flags seeded off. **Portal chrome v3** (gated `org_portal.chrome_v3`, seeded off, independent of v2): shadow-lift hover treatment + `lucide-react` icons on portal tiles/directory cards, and the portal's first footer (org contact info, nav recap, copyright), via a new RLS-verified `getOrgProfileForFooter()` reader. The officers tile has its own `PORTAL_TILES` entry (reachable from both the tile grid and the persistent nav row, not just direct URL); the directory's Members/Households/Parishes switcher is now a connected `ButtonGroup` (`src/components/shared/button-group.tsx`, dependency-free) with icons; the whole `(org)` shell (header/main/footer) widened from `max-w-4xl` to `max-w-6xl`; sign-out now lands on the org's own public site (`/site/<slug>`) instead of the platform home. First tenant-facing capabilities a congregation's own staff — not a platform admin — can act on. **Organization Administration hub** (gated `org_portal.admin_hub`, seeded off): a new `/o/<slug>/admin` index page, reached from the persistent nav's flag-gated "Administration" entry, listing every setup-category destination as a flat card grid (fpcw-directory pattern — no sidebar). `PORTAL_TILES` gained a `category: "operate" | "administer"` discriminant (pure routing, never a second permission gate — DECISION-003): `roles`/`features`/`branding` are `"administer"` (the `roles` tile's own label renamed `"Administration"` → `"Roles"` to avoid colliding with the hub's title); `directory`/`members`/`officers`/`feedback` are `"operate"` on the main portal page, and `tickets` is `"administer"` — DECISION-105's same-day correction: members/officers are routine congregational work, filing a support ticket is closer to org setup. The hub itself performs no permission check of any kind — every flag-enabled administer tile renders regardless of the viewer's own grants, and each destination page remains the sole authority on access. The tile grid's CTA (`Button` `variant="tile"`) is a `bg-card` surface with a border and hover-lift shadow, not a solid brand-color fill — the brand color lives in a small icon badge per tile (DECISION-105, superseding DECISION-104's original full-bleed treatment). **Tenant-facing branding** (`/o/<slug>/admin/branding`, its own hub tile, gated `org_portal.branding` + a new `branding.manage` permission bound to a new `brand_admin` role — deliberately not `stated_clerk`, DECISION-101): an org admin can now set their own congregation's seed color, type pairing, logo, and light-only toggle — branding was platform-admin-only before this. `withOrgContext()`-only, no schema change; a new `TENANT_BRAND_SET` audit key distinct from the platform's own `ORG_BRAND_SET`; "neutralize" and the platform admin's own brand form both stay live, unchanged, as an override path. `src/lib/directory.ts`, `src/lib/role-grants.ts`, `src/lib/tickets.ts`, `src/lib/officers.ts`, `src/lib/tenant-branding.ts`, `src/components/org-portal/portal-footer.tsx`, `src/components/shared/button-group.tsx`, `src/app/(org)/o/[slug]/{directory,admin/roles,admin/officers,admin,admin/branding,tickets,feedback}/`, `docs/work-log/2026-08-26-portal-reorg-and-modernization.md`, `docs/work-log/2026-08-26-tenant-branding-permission.md`.
- **presby: roll** — `presby_roll_as_of`, `_counts_as_of`, `_changes`, `presby_reconcile_current_roll`, `presby_roll_cache_drift`; officer registers `presby_officer_roster` / `_history`.
- **presby: developer reference** — `/developer` index, `/developer/tables/<name>`, `/developer/erd/<module>`, `/developer/schema.json`. Generated from the schema + Postgres `COMMENT ON`.
- **presby: public sites** — `/site/<slug>` (P3, SHIP WITH NOTES) — anonymous, git-staged per-congregation websites. Content lives in a private `site-<slug>` GitHub repo per congregation; CI (inherited from the public `presby-site-kit` dependency) validates and stages a structured bundle, then POSTs it to `POST /api/sites/ingest`, authenticated via GitHub Actions OIDC (hardcoded RSA-SHA256 verification, no callout to GitHub at request time). `organization_sites` has no `presby_app` grant at all — reachable only through `presby_published_site()` (SECURITY DEFINER), which collapses never-provisioned/suspended/nonexistent/inactive into one indistinguishable zero-row 404. `site_contact_messages` — anonymous submissions, honeypot + IP/slug rate limit, surfaced on the existing `/o/<slug>/tickets` page gated `tickets.file`. Gated `sites.public_render`, **seeded off — no real congregation site is provisioned yet**; the mechanism is fully built and tested (unit, real-Postgres integration, e2e, and a genuine end-to-end run — real GitHub Actions OIDC ingest into a real running server), not the rollout. `organization_profiles`/`organization_service_times` (schema landed, admin-editable at `/admin/organizations/[id]`, `FEATURES.ADMIN_ORGANIZATIONS`) add address/phone/five named social-link columns/structured service-times-and-office-hours to the same `presby_published_site()` collapse. `presby-site-kit` is now a real component library (v3.4.0, external repo) — twelve components (Hero, FeatureGrid, Callout, ServiceTimes, StaffList, ValuesGrid, MinistryList, EventList, SermonEmbed, DonateLink, Prose, Gallery — a single-image-at-a-time auto-playing carousel with play/pause + pause-on-hover/focus + `prefers-reduced-motion` support, the package's second client component after `Nav`) plus Footer chrome, rendered from a page's typed content blocks against a fixed allowlist (never MDX-with-embedded-JS — a content repo is a lower trust tier than presby's own code); `FeatureGrid`/`ValuesGrid`/`MinistryList` items carry an optional per-item image (`imageUrl`/`imageAlt`), mirroring `StaffList`'s `photoUrl`. First real (non-fixture) content repo: `github.com/chenson42/site-fpcw` (private, First Presbyterian Church of Westerville, slug `fpcw`) — 14 pages migrated by hand from a real WordPress export, not yet ingested (org not provisioned yet, `images/` not yet populated — see `site-fpcw/MANIFEST.md`). SEO/embed parity from hands-on fidelity testing: per-organization `robots.txt` (`Allow: /site/`, everything else disallowed, `src/app/robots.ts`) + `/site/<slug>/sitemap.xml` (`buildSitemapEntries()`, lists only pages that opt into `Nav` via `frontMatter.navLabel`); real per-page `<title>` via `generateMetadata()` (was falling through to the platform's own "presby" title) with `getPublishedSite()` now request-memoized; CSP `frame-src` allows the two trusted video-embed hosts (`youtube-nocookie.com`, `player.vimeo.com`); the portal-login nav link can fold into a content-authored nav group (`portalNavGroup`/`portalLabel`) instead of always rendering flat (`docs/work-log/2026-08-24-public-site-parity-fixes.md`). `src/lib/sites.ts`, `src/app/api/sites/ingest/route.ts`, `src/app/(public)/site/[slug]/`, `docs/work-log/2026-08-20-public-sites.md`, `docs/work-log/2026-08-21-public-site-org-profile.md`.
- **presby: children's ministry** — Increment A (roster + guardian links): `/o/<slug>/admin/members/children` age-computed roster (no-DOB rows excluded, disclosed in the empty state) + staff-entered guardian/emergency-contact links at `/admin/members/<id>/edit/guardians` (existing-person typeahead first, free-text fallback, XOR-validated). New tier-2 `children.roster` permission (catalog description discloses the full-DOB unmasking, DECISION-114) on a new `children_ministry_admin` role, separate from `member_care_admin` (DECISION-111); first-ever app-level gating on `person_relationships`. Gated `org_portal.children_ministry` (seeded off). Increments B (consent/medical intake) and C (check-in, re-scoped against the events contract) unbuilt. `src/lib/children.ts`, `docs/work-log/2026-08-26-childrens-ministry.md`.
- **presby: events** — Increments 1–2: flat, composite-keyed, FORCE-RLS `events` table (`src/lib/db/domain/events.ts`, migration 0036) with the frozen six-column check-in contract (DECISION-113) and materialized recurrence (`parent_event_id` + `recurrence_pattern`/`recurrence_count`, 52-occurrence series cap — DECISION-115; pattern edits extend forward, never rewrite existing occurrences). Admin CRUD at `/o/<slug>/admin/events` (create single/series, edit, cancel — no delete, extend series), gated `org_portal.events` flag (seeded off) + a new `events.manage` permission (no default role binding — DECISION-115). `is_public` column present but inert until Increment 4 (public calendar); Increment 3 is children's-ministry check-in. `src/lib/events.ts`, `src/lib/events/recurrence.ts`, `docs/work-log/2026-08-26-events-model.md`.
- **presby: presbytery credentials** — Increment 0+2 of the presbytery-parity program: ministerial credentials at `/o/<slug>/admin/credentials` (presbytery orgs) — record ordinations, change standing vs. end ordination as two distinct UI controls over one gated write path (status changes never touch `endedOn` — ordination is lifelong), record/end pastoral appointments in a new presbytery-owned `appointments` table (composite FK forces presbytery ownership; serving org validated against the presbytery's own member congregations, enumeration-safe). New `ordinations.status` column, `credentials.manage` permission (tier 2), `presbytery_stated_clerk` template role — the first non-null `organization_type_scope` template, invisible/unadoptable from a congregation (regression-tested). Four audit keys. Gated `org_portal.credentials` (seeded off). Migration 0037. The generic portal (directory/members/groups/officers/feedback/branding) is verified working for presbytery orgs, with congregation-assuming copy fixed in four spots. Increments 1 (committees/commissions) and 4b (dashboard/rollups incl. the viability map) remain deferred — `docs/TODO.md`. `src/lib/credentials.ts`, `docs/work-log/2026-08-26-presbytery-functionality.md`.
- **presby: presbytery oversight & statistics** — Increments 3+3b of the presbytery program: `congregation_oversight` (viability score, buildings/insurance — presbytery-owned judgment about ANY member congregation, managed or not, no publication dependency — this is how Increment 3's "Q1" cross-org-read question dissolved: it was never a downward read) and `congregation_statistics` (SASR-shaped annual aggregates, a `provenance` column — `presbytery_entered` / `published_by_congregation` / `imported` — coalesced at read time; publication wins for a given congregation+year but a presbytery-entered row is never deleted) at `/o/<slug>/admin/oversight` and `/admin/reports` (presbytery-only, replacing the portal-IA coming-soon stubs on the same `org_portal.oversight`/`.reports` flags, now graduated to seeded-off). `per_capita_rates`/`per_capita_records` derive billed amounts from the coalesced statistics at a presbytery-set `basisYear` (defaults to billing-year-minus-2, the real PC(USA) arrears convention), snapshotting the count at generation time so a later correction can't retroactively move an issued bill. The platform's first cross-org write — `presby_publish_sasr_snapshot()` (SECURITY DEFINER, called from the congregation's own context, derives its presbytery target via `parentId`, never accepts one as a parameter) — ships in this migration but its congregation-side UI (Increment 4a) is not yet built; the read/coalesce side is fully live. A freeze trigger rejects UPDATE/DELETE on published rows; corrections chain via `supersedesPublicationId`. New permissions `congregation_oversight.manage`/`statistics.manage`/`per_capita.manage`/`statistics.publish` (tiers 1-2). **Known scope gap:** all three surfaces exclude `new_worshiping_community` congregations (narrower than Increment 2's appointments precedent) — tracked in `docs/TODO.md` as an open product question, not a defect (fails closed, documented). The `(public)/site/<slug>` fallback (DECISION-121) also ships here: a presbytery/synod/GA with no provisioned site renders an org-name-plus-sign-in-link page instead of a bare 404; congregations keep the unchanged enumeration-safe 404. `src/lib/db/domain/presbytery.ts`, `src/lib/presbytery.ts`, migration `drizzle/0038_presby_presbytery_program.sql`, `docs/work-log/2026-08-27-presbytery-program.md`, `docs/work-log/2026-08-27-presbytery-oversight-statistics.md`.
- **presby: portal IA** — the org portal is domain-organized (DECISION-117): every `PortalTile` carries a required `domain` (People & Membership / Worship & Events / Giving & Finance / Governance & Courts / Reports & Insights / Communications / Administration); home and the admin hub render labeled domain sections (`domain-tile-sections.tsx`); the nav row shows one anchor entry per domain with ≥1 visible operate tile (+ Home + Administration, ~8 entries regardless of tile count). Planned areas ship as flag-gated coming-soon pages under `/o/<slug>/admin/{giving,worship,insights,communications,committees}` (`committees` presbytery-scoped; flags seeded ON in dev, MUST flip off before go-live — tracked in TODO). **`oversight` and `reports` have since graduated from coming-soon stubs to real features** (see "presbytery oversight & statistics" above) — their flags are now seeded off, the production convention. "Give feedback" is no longer a tile/nav/footer entry — it's an avatar-menu item plus the shared dismissible feedback-prompt card at the bottom of the portal home (`src/components/shared/feedback-prompt-card.tsx`, reused from the platform home; `org_portal.feedback` unchanged). An operator taxonomy revision (Officers→People; Events+Communications merge; new order) is approved and queued. `src/lib/org-portal/tiles.ts`, `docs/work-log/2026-08-27-product-ia-scaffold.md`.
- **presby: NOT built** — ledger/giving, worship, check-in, public event calendar (coming-soon stubs exist for these — see portal IA above). Within schema/authorization: roll UI (read path complete, unsurfaced), new-role/new-permission creation, the cross-org commission/delegation UI, and a tenant-facing audit reader (DECISION-067, deferred pending a FORCE-RLS `audit_events` projection) remain unbuilt.
- **presby: post-login routing** — `/launch` computes a nine-row destination matrix and forwards; `/home` is the chooser (merged with the deleted `/orgs`, never auto-forwards); `/no-organization`; `/o/<slug>` org shell in the `(org)` group. Slug is immutable, DNS-label constrained. Named access-denied that is byte-identical across platform statuses (DECISION-040/044).
- **presby: header controls** — avatar menu (identity: account, `/admin` and `/developer` when entitled, sign out) and organization switcher (context), split on Google's model. `src/components/shared/{avatar-menu,org-switcher,global-nav}.tsx`.
- **Design system** — `cn()`, `components.json`, generated primitives (button/card/badge/dropdown-menu/table/input/label/textarea/alert-dialog), closed token contract, `.dark` class scheme (not media-query). **Per-organization branding is live in `(org)`** (P0.5, gated `ui.brand_theming`): operator sets a congregation's colour/logo/type pairing at `/admin/organizations`; `(org)` emits it as a `:root`-scoped `<style>` reaching Radix portals; DECISION-040 denial pages and every other route stay platform-default by construction. `(public)/site/<slug>` (anonymous) emission is live but flag-gated off (`sites.public_render`) — see presby: public sites.
- **Public / Auth** — landing page, sign-in (Google OAuth + credentials, Turnstile-guarded, lockout-aware; org-branded — logo/colors/fonts, Google button pinned to platform colors — when reached via a live public site's callback, gated `ui.branded_signin` seeded off, DECISION-094), TOTP 2FA verify + trusted device, forgot/reset password, email-change verify landing, access-pending. Credentials sign-in now evaluates the 2FA predicate and redirects itself (`/totp` vs. destination) rather than relying on the Edge gate seeing every request — a same-hop callbackUrl could previously soft-navigate past it entirely.
- **presby: post-login router** — `/launch` decides and redirects (nine-row matrix as a pure function), `/home` chooser (org cards + Platform block, never auto-forwards; `/orgs` is a permanent redirect to it), `/no-organization` zero-org funnel, `/o/<slug>` org landing stub in the new `(org)` group with the four-way miss response (enter / ended / denied / 404).
- **Member** — `/home` is the post-login destination again (merged with the former chooser, 2026-08-27): greeting, "Your organizations" cards, Platform block, roles/features, what's-new card, daily feedback prompt, `/whats-new` full list, feedback submit/snooze/opt-out actions.
- **Account (self-serve)** — profile name, email change + re-verification, password change, per-user TOTP enrolment/manage at `/account/2fa`, delete-account skeleton, permanent feedback form.
- **Admin (`/admin`)** — redesigned 2026-08-27 as a domain-sectioned tile grid mirroring the org portal's own design language (`src/lib/admin-portal/tiles.ts`, DECISION-123): tiles are hidden, not shown-then-denied, when the viewer lacks the held `FEATURES.*` key (a pure synchronous registry — no session/query inside it, filtering happens once at the page) — this also fixed two real defects found in the redesign: every card used to render for any `ADMIN_DASHBOARD` holder regardless of what they actually held, and `FEATURES.ADMIN_TICKETS` had no tile at all. Users + roles (+ lock badge/unlock, 2FA reset), feature flags, release-notes docs viewer, per-congregation 2FA policy, audit viewer, feedback triage, what's-new CRUD, email-queue viewer + retry, tickets. `docs/work-log/2026-08-27-platform-home-and-portal.md`
- **Auth backend** — NextAuth 5 config, cached session, safe callbackUrl, lockout, sign-in gate, local-login flag, session projection; edge gate `src/proxy.ts` (admin + 2FA).
- **Platform lib** — permissions (`FEATURES` + `hasFeature`), flags (`isFlagEnabled`, cached), `recordAudit()`, TOTP crypto + pending enrolment, rate limiting, request-ip, Turnstile, email queue (persist-first + retry + Resend webhook) with `escapeHtml`.
- **API / Cron** — NextAuth routes, Resend delivery webhook, `CRON_SECRET`-gated email-queue worker + daily maintenance (token GC).
- **Flags** — `platform.merged_home` (the /home+/orgs merge, seeded on — a live ship), `auth.local_login` (OAuth-only switch), `auth.require_2fa` (install-wide 2FA master switch; per-congregation policy is `organization_settings.require_two_factor`, not a flag) — both auth flags fail-open. (`demo.new_dashboard` retired 2026-08-27 alongside the admin-portal redesign.)
- **Dev-loop tooling** — SessionStart hooks (feedback count, functionality-map index, cadence check), pre-push PreToolUse gate, commit-msg hook (+ `Work-Log:` trailer) + escape-rate stats, `check:audit` + `check:sql-date` tripwires, CI (typecheck/build/tests/commit-grammar + secret-gated Neon-branch e2e + opt-in Claude PR review + dependabot), seed script, 11 e2e suites, fork-sync skills (`/upstream-sync` + `/downstream-sync`) + contribution-kit specs, `AGENTS.md` shim.

---

## Public / Auth

- Landing page — public marketing home, redesigned 2026-08-27: a full-bleed hero (PresbyPortal's connectional-courts framing — a hand-drawn Congregation→Presbytery→Synod→General Assembly diagram, the platform's own organizing principle, not generic iconography), a capabilities section grounded in this map's own shipped/not-shipped list, never redirects a signed-in user (DECISION-034). Uses a new, documented one-page `hero` type-scale exception (docs/ui-standards.md) since the app's internal 7-role scale tops out at 30px. `src/app/page.tsx`, `docs/work-log/2026-08-27-platform-home-and-portal.md`
- Post-login router — `/launch`, pure `computeDestination()` over nine rows; the chooser row's target is `/home` (was `/orgs` — DECISION-124). `src/app/launch/page.tsx`, `destination.ts`
- **`/orgs` is gone** — a permanent (308) `next.config.ts` redirect to `/home`; its route segment was deleted, not stubbed.
- Not-connected page — `/no-organization`, copy differs for an org still being set up. `src/app/no-organization/page.tsx`
- Organization shell — `/o/<slug>`, `(org)` group; `withOrgContext` only, `getPlatformDb()` forbidden. `src/app/(org)/o/[slug]/`
- Sign-in — Google OAuth + credentials, Turnstile, lockout-aware, safe `callbackUrl` → `/launch`; issues its own `/totp` redirect from the 2FA predicate rather than depending on the Edge gate seeing the next request (fixes a same-hop-callbackUrl bypass). Org-branded when `callbackUrl` names an org with a live public site (`getPublishedSiteBrand()`, gated `ui.branded_signin` seeded off) — enumeration-safe (mirrors `/site/<slug>`'s own collapse), Google button excluded via `[data-brand-neutral]`. `src/app/(auth)/signin/page.tsx`, `actions.ts`, `parse-org-slug.ts`
- TOTP verify — code entry + trusted-device cookie; enrolment redirect two-hop (proxy → `/totp` → `/account/2fa`). `src/app/(auth)/totp/page.tsx`, `actions.ts`
- Forgot/reset password — request link + consume token (hashed at rest, enumeration-safe). `src/app/(password-reset)/forgot-password/page.tsx`, `reset-password/page.tsx`, `actions.ts`
- Email-change verify — token landing with error boundary. `src/app/(email-verify)/account/verify-email/[token]/page.tsx`
- Access-pending — authenticated, no roles; writes `ACCESS_DENIED` audit on bounce. `src/app/access-pending/page.tsx`

## Post-Login router

- `/launch` — the single post-authentication target. Reads the org list + `users.is_platform_admin`, calls `computeDestination()`, redirects; renders only the database-unreachable state. `src/app/launch/page.tsx`, `destination.ts`
- **`/home` is now the chooser** (merged with the former `/orgs`, 2026-08-27 — DECISION-124/125): one card per enterable organization (name + type, **no membership language**, unchanged), a Platform block (Admin card iff `canAccessAdmin`, Developer card iff `isPlatformAdmin` — two independently-evaluated conditions, never one shared boolean, DECISION-044), a notice for organizations still being set up, plus the pre-existing what's-new card and daily feedback prompt below it. Never auto-forwards. Gated `platform.merged_home` (seeded **on** — this is a live ship, not a dark rollout; OFF renders the pre-merge content). `src/app/(member)/home/page.tsx`
- `/no-organization` — five-state zero-org funnel (already has access / being set up / not a tenant / access ended / never connected) plus two doors. `src/app/no-organization/page.tsx`
- `/o/<slug>` — org landing stub + the four-way miss response; `resolveOrgContext()` then `assertOrgAccess()`. `src/app/(org)/o/[slug]/page.tsx`, `org-states.tsx`, `error.tsx`, `not-found.tsx`

## Org portal (`(org)` group, tenant-facing content)

- Portal home — `/o/<slug>`, gated `org_portal.home_v2` (seeded off; flag off → the original stub): time-aware greeting, find-a-person server action (`directory.view` re-checked, single-match jump), "Yours" household zone, flag-mirroring tile grid. `src/lib/org-portal/`, `src/app/(org)/o/[slug]/page.tsx`
- Directory — `/o/<slug>/directory`, membership-scoped, privacy-filtered read (`person_privacy` field-level hides applied in SQL). Gated `org_portal.directory` flag + `directory.view` permission; **v2** (gated `org_portal.directory_v2`, seeded off, flag off → the original flat list) adds a searchable card grid, households view, person/household detail pages, and a `directory.view_hidden`-gated parishes/deacon-roster view (deacon derived from `officer_terms.org_unit_id`, DECISION-095). `src/lib/directory.ts`, `src/app/(org)/o/[slug]/directory/page.tsx`, `docs/work-log/2026-08-24-portal-home-directory.md`
- Portal chrome — org logo (`OrgWordmark`, links to `/site/<slug>`) replaces the platform wordmark in the header inside `/o/<slug>`, plus a persistent flag-filtered menu row. Gated `org_portal.chrome_v2` + `org_portal.feedback` (both seeded off). **v3** (gated `org_portal.chrome_v3`, seeded off, independent of v2): shadow-lift hover treatment + `lucide-react` icons on portal tiles/directory cards, and the portal's first footer (org contact info, nav recap, copyright), via a new RLS-verified `getOrgProfileForFooter()` reader. `src/app/(org)/o/[slug]/layout.tsx`, `portal-nav.tsx`, `src/components/org-portal/portal-footer.tsx`, `docs/work-log/2026-08-25-portal-chrome.md`, `docs/work-log/2026-08-26-portal-fpcw-directory-ux.md`
- Role administration — `/o/<slug>/admin/roles`, grant/revoke/view existing `role_grants` rows for existing `app_roles` (`role_grants.manage`, bootstrap role `stated_clerk`, DECISION-066), **plus role *definition***: create a custom role, edit an existing custom role's permission set (tier-grouped checklist), deactivate one (soft, never a hard delete — ends its live grants atomically), or adopt a stock template — all gated on a separate new `roles.manage` permission bound to a new `role_admin` role, not `stated_clerk` (DECISION-106/109). The definition-side escalation check (`assertPermissionSubset()`, shared with `grantRole()`'s own subset check) and self-lockout guard close the exact gap a definition UI opens that assignment alone doesn't. Constitutional roles (`isProtected`) stay read-only through this UI. Same `org_portal.roles` flag gates both halves. `src/lib/role-grants.ts`, `src/lib/role-definitions.ts`, `src/lib/authz.ts`, `src/app/(org)/o/[slug]/admin/roles/`, `docs/work-log/2026-08-26-role-permissions-admin.md`
- Support tickets — `/o/<slug>/tickets*`, `/o/<slug>/feedback`; file/track/reply on a ticket, baseline-member feedback on-ramp promotable into a ticket by the `tickets.file` role-holder. Categories (`change_class`) + area + priority, PNG/JPEG/WEBP/PDF attachments (10MB, magic-byte sniffed), five email-notification triggers. Gated `org_portal.tickets` flag (one flag, both surfaces) + `tickets.file` permission (bootstrap role `support_contact`, DECISION-080). No autonomous AI actor — resolution is an ordinary human-plus-Claude-Code engineering task. `src/lib/tickets.ts`, `src/lib/tickets-notifications.ts`, `src/app/(org)/o/[slug]/{tickets,feedback}/`.
- Officer-terms administration — `/o/<slug>/admin/officers` + `/officers/[personId]`; record/end a session/diaconate officer term (person + office + dates, deacon-only district field), current roster, per-person history. Start/end only — no delete, since `group_memberships.officer_term_id` is an unconstrained FK. Exclusion-constraint (`officer_terms_no_overlap`) and check-constraint violations map to specific copy rather than a raw DB error. Gated `org_portal.officers` flag + `officers.manage` permission, bound to the existing `stated_clerk` role — no new role minted; `officer_terms.office = 'clerk_of_session'` stays a data value only, never automatically granting the permission (the ecclesiastical office and the software grant are deliberately unlinked). Application-layer regression test proves the write path doesn't reopen F22's bug class (a second non-consecutive term rewriting the first's end date). `src/lib/officers.ts`, `src/app/(org)/o/[slug]/admin/officers/`, `docs/work-log/2026-08-26-groups-and-officers.md`.
- Children's ministry — `/o/<slug>/admin/members/children` roster (everyone under 18 with age/grade computed from `dateOfBirth`; a person with no birthdate on file is excluded, disclosed in the empty state) and per-child guardian/emergency-contact management at `/admin/members/<id>/edit/guardians` (link an existing directory person or record a free-text name — XOR-validated server-side; relationship kinds: parent/guardian/emergency contact). Gated `org_portal.children_ministry` flag (seeded off) + `children.roster` permission (tier 2, bound to `children_ministry_admin` — deliberately separate from `member_care_admin` so the roster never implies medical access; permission description discloses that holders see full birthdates even when directory-hidden). First application-level surface over the global `person_relationships` table. `src/lib/children.ts`, `docs/work-log/2026-08-26-childrens-ministry.md`.
- Presbytery credentials — `/o/<slug>/admin/credentials` (presbytery organizations): record a minister's ordination, change their standing (active/honorably retired/on leave/etc. — a status change never ends the ordination; "End ordination" is a separate, removal-only control, both funneling into one `changeOrdinationStatus` write path per DECISION-116), and record/end pastoral appointments (who serves as pastor of which member congregation — the serving-org picker offers only the presbytery's own member congregations, `parentId` + org-type validated, enumeration-safe `invalid_target` on miss). A minister must hold current membership at the presbytery first; the forms block with guidance and a Members link otherwise (DECISION-116, no inline create). Four audit keys (`ORDINATION_RECORDED`/`ORDINATION_STATUS_CHANGED`/`APPOINTMENT_RECORDED`/`APPOINTMENT_ENDED`). Gated `org_portal.credentials` flag (seeded off) + `credentials.manage` permission, carried by the `presbytery_stated_clerk` template role — the first `organization_type_scope`-non-null template; congregations can neither see nor adopt it. The portal tile itself is also org-type-scoped (`PortalTile.orgTypeScope`, allow-listed to presbytery, honored across all four rendering surfaces — tile grid, admin hub, nav row, footer — via `visiblePortalTiles()`'s required `organizationType` parameter), and a non-presbytery org hitting the URL directly gets an honest "not available for this kind of organization" state, not the permission-denied copy (2026-08-27 bug fix, `docs/work-log/2026-08-27-credentials-tile-org-type.md`). `src/lib/credentials.ts`, `src/app/(org)/o/[slug]/admin/credentials/`, migration `drizzle/0037_presby_ministry_credentials.sql`, `docs/work-log/2026-08-26-presbytery-functionality.md`.
- Events administration — `/o/<slug>/admin/events`: create a single event or a recurring series ("This event repeats" reveal; weekly / monthly-by-weekday patterns, count-bounded, 52-occurrence series-total cap — DECISION-115), edit title/description/location/times/visibility, cancel (soft — `cancelled_at`, no delete), and extend a series forward (pattern edits never move or rewrite occurrences already materialized — disjoint write paths enforce it). Events are materialized rows in a flat `events` table carrying the frozen six-column check-in contract (DECISION-113) that children's-ministry check-in (Increment 3) and the public calendar (Increment 4, reads `is_public`) will consume. Gated `org_portal.events` flag (seeded off) + `events.manage` permission (no default role binding). `src/lib/events.ts`, `src/lib/events/recurrence.ts`, `src/app/(org)/o/[slug]/admin/events/`, `docs/work-log/2026-08-26-events-model.md`.

## Brand / per-organization theming (P0.5)

- Closed token contract — brandable/bounded/platform partition, legal contrast pairs (D1–D6), type scale, four curated type pairings (classic/modern/warm/**contemporary** — Montserrat/Open Sans, DECISION-093 — an exact match for a real congregation's identity rather than an approximation; open per-admin font selection was considered and explicitly rejected); zero runtime imports. `src/lib/brand/contract.ts`
- Ramp generator — seed hex → independently-derived light + dark token sets + `adjustments[]`, property-tested against every OKLCH seed, versioned (`BRAND_TOKEN_VERSION`). `src/lib/brand/generate.ts`
- Storage — `organization_brands` (one row per org, FORCE RLS, no public grant ever) + `organization_brand_history`; set/neutralise by the platform operator only (`org.branding`'s tenant-facing editor is still P1-blocked). `src/lib/db/domain/org.ts`
- `(org)` emission — **live**, gated by `ui.brand_theming`. `(org)/o/[slug]/layout.tsx` resolves the caller's own membership, reads the org's brand (null-safe: flag off / not a member / never branded all render the platform default), and `<BrandTokens>` emits both colour schemes as one `:root`-scoped `<style>` element — reaches Radix portals, not just the layout's own DOM subtree. `src/lib/brand/read-org-brand.ts`, `src/components/brand/brand-tokens.tsx`
- Un-brandable by construction — the DECISION-040 access-denied/ended/404 pages and every route outside `(org)`/`(public)/site/<slug>` never receive brand tokens; enforced by `scripts/check-brand-scope.mjs` (E1–E3, C1–C2 all live tree-wide as of `a8`/`c4`).
- Font pairing — per-org heading/body face resolved to self-hosted `next/font/google` faces; applied to `(org)`'s body text today (heading-face differentiation not yet wired — see `docs/TODO.md`). `src/lib/brand/fonts.ts`
- `(public)/site/<slug>` emission (anonymous visitors) is **live but flag-gated off** — see Public websites (P3) below.
- Light-only mode — `organization_brands.light_only`; when set, both `(org)` and `(public)/site/<slug>` force the light palette even when a visitor's OS/browser prefers dark, for a congregation whose real identity has no dark variant. Admin toggle on the same brand form, folded into the existing `ORG_BRAND_SET` audit event. `src/lib/db/domain/org.ts`, `docs/work-log/2026-08-24-light-only-brand.md`

## Public websites (P3)

- `/site/<slug>` — anonymous per-congregation website, gated `sites.public_render` (seeded off; no real congregation site provisioned yet). `getPublishedSite()` reads through `presby_published_site()` (SECURITY DEFINER) — the only tenant-connection path to `organization_sites`, which carries no `presby_app` grant at all. Enumeration-safe: never-provisioned, suspended, nonexistent, and inactive-org all collapse to the same zero-row 404, proven at the SQL, unit, integration, and e2e layers. `src/app/(public)/site/[slug]/`, `src/lib/sites.ts`.
- `presby-site-kit` (`github.com/chenson42/presby-site-kit`, public, `v0.0.1-stub`) — the shared rendering shell, pinned by git tag in `package.json`. Renders staged pages via `renderSiteBundle()`; a component allowlist (no arbitrary JS from content repos) is the enforced boundary.
- Ingest — `POST /api/sites/ingest`, called by a content repo's own CI on push to `main`. GitHub Actions OIDC only (hardcoded RSA-SHA256, `iss`/`aud`/`repository_owner`/`ref`/`job_workflow_ref` all checked, never trusts the token's declared `alg`), then the `sites.public_render` flag (503 if off — checked *after* auth, so a stolen token can't probe the flag), then bundle validation + per-image magic-byte sniffing, idempotent on commit sha. `src/app/api/sites/ingest/route.ts`, `src/lib/sites-ingest-auth.ts`.
- Contact form — anonymous `site_contact_messages` (FORCE RLS, composite tenant key), honeypot + IP/slug rate limit, read side on the existing `/o/<slug>/tickets` page gated `tickets.file` (DECISION-089). `src/lib/sites.ts`.
- Admin — `/admin/sites` provisions a site (repo string, `FEATURES.ADMIN_ORGANIZATIONS`) and sets status (provisioning/live/suspended).
- Open before real rollout: `site-<slug>` content-repo visibility (public vs. private) is undecided and matters for the enumeration-safety guarantee once real repos exist; Vercel build-sandbox and payload-size verification for the git dependency are unverified — `docs/TODO.md`.

## Member

- Home — **the post-login destination again**, merged with the former `/orgs` chooser 2026-08-27 (see Post-Login router — this reverses what this line said before that date). Greeting, "Your organizations" cards, Platform block, what's-new card, daily feedback prompt card (UTC-read/local-write, DECISION-023). `src/app/(member)/home/page.tsx`, `src/components/shared/feedback-prompt-card.tsx`
- What's-new — full entry list, newest-first. `src/app/(member)/whats-new/page.tsx`
- Feedback actions — submit / snooze / opt-out (rate-limited; per-user prompt state, one column per upsert). `src/app/(member)/feedback/actions.ts`

## Account (self-serve)

- Account page — display name, email change (re-verify), password change, delete skeleton, permanent feedback form. `src/app/(account)/account/page.tsx`, `actions.ts`
- Per-user 2FA — QR enrolment, verify, recovery codes, disable. `src/app/(account)/account/2fa/page.tsx`, `actions.ts`

## Admin (`src/app/(admin)/admin/`)

- Dashboard — domain-sectioned tile grid, permission-filtered (see the redesign note above). `admin/page.tsx`
- Users — list + role assignment, lock badge + unlock (audited). `admin/users/page.tsx`, `users/[id]/page.tsx`, both `actions.ts`
- Feature flags — toggle + rollout percent. `admin/flags/page.tsx`, `actions.ts`
- Docs — release-notes markdown viewer (`docs/release-notes/vX.Y.md`). `admin/docs/page.tsx`
- 2FA policy — per-congregation `require_two_factor` toggles + "required but not enrolled" list; platform connection (RLS hides orgs from the tenant connection); gated on `admin.two_factor` (DECISION-033). `admin/2fa/page.tsx`, `actions.ts`, `policy-toggle.tsx`
- Audit viewer — filter by action/actor, pure RSC. `admin/audit/page.tsx`; helpers `src/lib/audit-page-helpers.ts`
- Feedback triage — status new→triaged→done/declined; renders bodies as plain text (hostile-content invariant). `admin/feedback/page.tsx`, `actions.ts`
- What's-new CRUD — list+create, edit, delete; HTML-reject validation. `admin/whats-new/page.tsx`, `[id]/page.tsx`, `actions.ts`
- Email-queue viewer — send status, delivery status, retry. `admin/email-queue/page.tsx`, `actions.ts`
- Organizations — list + "still on default palette" (OQ4) filter, **create** (`/admin/organizations/new` — name/slug/type/platform_status, reserved-slug + DNS-label validation, `path` derivation, F16 derived-group seeding — session/diaconate/active_membership for congregations, active_membership only otherwise — in the same transaction so officer-term recording works immediately), detail page sets/previews/neutralises a congregation's brand (seed colour, logo, type pairing), audited (`ORG_CREATED`/`ORG_BRAND_SET`/`ORG_BRAND_NEUTRALIZED`). `admin/organizations/page.tsx`, `new/page.tsx`, `new/create-organization-form.tsx`, `new/actions.ts`, `[id]/page.tsx`, `[id]/actions.ts`, `[id]/brand-form.tsx`, `src/lib/org-provisioning.ts`, `src/lib/reserved-slugs.ts`
- Support-ticket triage — `/admin/tickets`, cross-org queue filterable by status/area/priority, status state machine, assign/reclassify/area/priority controls, operator reply. Reads via `getPlatformDb()` (tickets is FORCE RLS, unlike `feedback`). Gated `FEATURES.ADMIN_TICKETS`. `admin/tickets/page.tsx`, `[id]/page.tsx`, `actions.ts`

## Auth backend & edge

- NextAuth 5 config — Google OAuth + credentials, JWT sessions with roles/features/2FA claims. `src/auth.ts`, `src/lib/auth/config.ts`, `session-projection.ts`
- Cached auth — `cache()`-wrapped `auth()`/flags (one SELECT per request). `src/lib/auth/cached-auth.ts`
- Safe callback — same-origin path check, `/launch` fallback (pure string function; it does not learn about org slugs). `src/lib/auth/safe-callback.ts`
- Lockout — 5 failures → 15-min DB lock, enumeration-safe, OAuth-exempt. `src/lib/auth/lockout.ts`
- Sign-in gate + local-login flag — credentials gating incl. `auth.local_login` OAuth-only mode. `src/lib/auth/sign-in-gate.ts`, `local-login.ts`
- Edge route gate — auth + active status everywhere, 2FA on `/admin/*` and `/o/*`; deliberately no membership check on `/o/*` (DECISION-035). Edge runtime; must not import `@/lib/db`. `src/proxy.ts`

## Platform lib (`src/lib/`)

- Permissions — `FEATURES` catalog (`admin.dashboard/users/flags/release_notes/feedback/audit/email_queue/whats_new/organizations/two_factor/tickets`) + `hasFeature()`; `SUPPORT_OPERATOR_ROLE` bundle (`admin.tickets`+`admin.feedback`, narrower than admin, DECISION-080). `permissions.ts`
- Flags — `isFlagEnabled()` env toggles + rollout, request-cached. `flags.ts`
- Audit — `recordAudit()` (actor, IP, user-agent) + `AUDIT_ACTIONS` catalog. `audit.ts`; IP extraction `request-ip.ts`
- TOTP — AES-GCM encrypt/decrypt + verify (`AUTH_TOTP_ENCRYPTION_KEY`); pending-enrolment store. `two-factor.ts`, `totp-pending.ts`
- Rate limiting — in-memory sliding window (Upstash env swap-in). `rate-limit.ts`
- Turnstile — endpoint-level CAPTCHA verify, no-op until keyed. `turnstile.ts`; widget `src/components/shared/turnstile.tsx`
- Email — durable queue (persist-first, backoff retry, Resend delivery webhook), `escapeHtml()`. `email/queue.ts`, `send.ts`, `escape-html.ts`
- DB — Drizzle + Neon; schema is source of truth; unique-violation helper. `db/schema.ts`, `db/errors.ts`
- Shared UI — `<FormattedDate>` (TZ-safe, ESLint-enforced), `<FeedbackForm>`, global nav, fresh-recovery-codes. `src/components/shared/`

## API / Cron / Webhooks

- NextAuth handlers. `src/app/api/auth/[...nextauth]/route.ts`
- Resend delivery webhook — HMAC-verified (DECISION-028: webhooks verify own signatures under `api/webhooks/`). `src/app/api/webhooks/resend/route.ts`
- Email-queue worker — retry loop, `CRON_SECRET` Bearer. `src/app/api/cron/email-queue/route.ts`
- Daily maintenance — expired-token GC (password-reset, email-verify, pending TOTP). `src/app/api/cron/maintenance/route.ts`

## Feature flags (seeded)

- `platform.merged_home` — gates `/home`'s merged-with-`/orgs` content; OFF renders the pre-merge page. Seeded on.
- `auth.local_login` — credentials on/off (OAuth-only mode); fail-open.
- `auth.require_2fa` — org-wide 2FA requirement; fail-open.

## Dev-loop tooling

- SessionStart hooks — feedback count (count only, never body content) + functionality-map short index + overdue-review cadence check. `scripts/feedback-check.mjs`, `scripts/functionality-map.mjs`, `scripts/cadence-check.mjs`
- Pre-push gate — PreToolUse hook blocks in-session `git push` unless `/pre-push` stamped a HEAD-keyed marker (Rule 5 mechanized). `scripts/pre-push-gate.mjs`
- Commit standards — prefix + `fix:` trailers + `Work-Log:` trailer on feat/fix, enforced by git hook locally and re-validated on PRs in CI; 30-day escape-rate report. `scripts/commit-msg.mjs`, `validate-commit-range.mjs`, `stats-escape.mjs`, `install-hooks.sh`
- Tripwires — audit coverage of mutations, `sql<Date>` ban. `scripts/check-audit-coverage.mjs`, `check-sql-date.mjs`
- CI — typecheck/lint/build/tripwires/`npm audit`/unit tests + commit-grammar job on PRs; e2e on an ephemeral Neon branch (secret-gated); opt-in Claude PR review (secret-gated); dependabot grouped updates. `.github/workflows/ci.yml`, `e2e.yml`, `claude-review.yml`, `.github/dependabot.yml`
- Cross-tool shim — `AGENTS.md` points non-Claude agents (Cursor/Codex/Jules) at CLAUDE.md and the must-honor rules. `AGENTS.md`
- Seed — roles, `FEATURE_CATALOG`, demo + auth flags, seed users (admin / member / MFA-admin). `scripts/seed.ts`
- E2E — 11 Playwright suites (auth, admin, member, security headers, TZ dates). Fixture users are hardcoded in `e2e/support/users.ts` and provisioned by globalSetup (DECISION-032) — no env vars, no conditional skips; cached storageState + DB isolation guard + rate-limiter precondition. `e2e/`
- Fork sync — `/upstream-sync` pulls starter changes into a fork (14 d cadence); `/downstream-sync` surfaces fork improvements to contribute back (30 d; self-detects the canonical repo and exits). `.claude/skills/upstream-sync/`, `.claude/skills/downstream-sync/`
- Contribution kit — ~40 origin→fix→verification specs contributed by the huddleup.health fork (PR #3); live status in the 2026-07-01 triage review. `docs/starter-contributions/README.md`

## Schema highlights (`src/lib/db/schema.ts`)

Auth/foundation: `users`, `accounts`, `sessions`, `roles`, `userRoles`, `features`, `roleFeatures`, `featureFlags`, `auditEvents`, TOTP tables (+ pending enrolments), `emailVerificationTokens`, `passwordResetTokens`, lockout state. Product/ops: `feedback`, `feedbackPromptState`, `whatsNewEntries`, `emailQueue`.
